{
  "name": "ob1-scripts",
  "private": true,
  "type": "module",
  "comment": "Types only. The scripts run under bun with no packages of their own; @types/bun, typescript and @types/node are pinned at server-portable's versions, which check 18 holds across the type-checked directories (SMD-1870).",
  "devDependencies": {
    "@types/bun": "1.4.0",
    "@types/node": "26.6.2",
    "typescript": "5.9.3"
  }
}
