# Fork-local CI.
#
# Upstream runs no tests at all: 11 test files across ~85k lines of code and no
# workflow invokes any of them. That is how server/test-stateless.mjs came to
# assert the opposite of production for three months without anyone noticing.
#
# This workflow exists so the fork's patch series cannot rot the same way. It is
# additive — it touches no upstream workflow — so it survives a rebase onto
# upstream/main untouched.
name: Fork Checks

on:
  push:
    branches: [main, "siggymd/**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fork-checks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-tests:
    name: Server tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.0"

      # bun.lock pins the full transitive tree; package.json pins the direct deps
      # exactly to server/deno.json, so the tests exercise the same libraries the
      # Edge Function deploys with. (`npm install && npm test` also works for
      # anyone without bun — the suites are plain node-compatible ESM.)
      - name: Install pinned test dependencies
        working-directory: server
        run: bun install --frozen-lockfile

      # Three suites, each carrying a drift guard that reads server/index.ts and
      # fails if the behaviour it asserts is no longer what the server implements.
      - name: Stateless transport + auth contract
        working-directory: server
        run: bun test-stateless.mjs

      - name: thought_stats pagination
        working-directory: server
        run: bun test-stats-pagination.mjs

      - name: capture_thought atomicity
        working-directory: server
        run: bun test-capture-atomicity.mjs

  repo-consistency:
    name: Repo consistency
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      # Applies the upstream gate's rules to the WHOLE repo, not just the
      # directories a PR happens to touch. The upstream gate only validates
      # changed dirs, so pre-existing violations never surface there.
      - name: Check metadata, links, dependencies and SQL guards
        run: node scripts/check-fork-consistency.mjs

  deno-check:
    name: Edge Function typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      # The Node suites cannot import index.ts (Deno.env at module scope, jsr:
      # imports), so this is the only thing that typechecks the file as deployed.
      #
      # --node-modules-dir=none resolves imports through deno.json exactly as
      # Supabase does. Without it, a stray node_modules/ (bun creates one in
      # server/ when running the test suites locally) flips Deno into manual
      # node_modules mode and the check fails with "Could not find a matching
      # package for 'npm:@supabase/supabase-js'". The flag makes this behave the
      # same in CI and on a developer machine that has just run the tests.
      - name: deno check server/index.ts
        working-directory: server
        run: deno check --node-modules-dir=none index.ts

  portable-server:
    name: Portable server (Bun + Workers)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.0"

      - name: Install
        working-directory: server-portable
        run: bun install --frozen-lockfile

      # Imports the real server rather than a mirror of it, so there is no drift
      # guard here and none is needed.
      - name: Test against the real server
        working-directory: server-portable
        run: bun test-server.ts

      - name: Typecheck
        working-directory: server-portable
        run: bunx tsc --noEmit

      # Proves the Cloudflare target still builds under the real Workers bundler.
      # Catches a dependency pulling in a node: builtin Workers cannot provide.
      - name: Cloudflare Workers build
        working-directory: server-portable
        run: bunx wrangler deploy --dry-run --outdir=.cf-out
        env:
          CLOUDFLARE_API_TOKEN: ""
          WRANGLER_SEND_METRICS: "false"

  db-schema:
    name: Schema migrations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.0"

      - name: Install
        working-directory: db
        run: bun install --frozen-lockfile

      # PGlite is real PostgreSQL 17 + pgvector in WASM, so this needs no service
      # container: the migrations are applied and asserted against a real planner.
      - name: Apply migrations and assert the schema
        working-directory: db
        run: bun test-schema.ts

  db-live:
    name: Schema against real Postgres
    runs-on: ubuntu-latest

    # A service container, not with-postgres.sh — the runner provides the database
    # and Actions handles readiness. The local script exists for developers, who
    # mostly run podman.
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: ob1test
          POSTGRES_DB: ob1test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.0"

      - name: Install
        working-directory: db
        run: bun install --frozen-lockfile

      # Covers what PGlite structurally cannot: the migration runner over TCP,
      # driver-level jsonb binding (where the double-encoding bug lived), and
      # whether the planner actually reaches for the HNSW index.
      - name: Run the live suite
        working-directory: db
        run: bun test-live.ts
        env:
          DATABASE_URL: postgres://postgres:ob1test@127.0.0.1:5432/ob1test
