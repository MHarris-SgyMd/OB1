---
type: added
bump: patch
tickets: [SMD-1936]
migrations: []
---

## Changelog

Check 20 holds every `.sql` in the tree, `db/migrations/` included, to the SQL-safety guard rail read as statements: no `DROP TABLE`, `DROP DATABASE`/`DROP SCHEMA`, `TRUNCATE` with a table after it, or `DELETE FROM` whose statement has no `WHERE` — comments excepted, string literals read, a refusing `BEFORE TRUNCATE ON` trigger the rule applied; the rules are `db/config.mjs`'s `DESTRUCTIVE_SQL_RULES`, and CLAUDE.md and CONTRIBUTING.md name the check (SMD-1936).

## FORK

The SQL-safety guard rail is a check — a `.sql` file never destroys rows a brain already holds, read as statements rather than words by check 20 over every `.sql` in the tree, so 046's refusing trigger passes and a planted `TRUNCATE` fails by file and line (SMD-1936)

**What changed.** `db/config.mjs` gains `DESTRUCTIVE_SQL_RULES` and
`destructiveSqlIn()` beside `SUPABASE_SQL_RULES` and `supabaseIsmsIn()`, the
shape check 12 uses, declared in `config.d.mts`. Four rules over the
comment-stripped text (`stripSqlComments`, literal-aware): `drop-table` for
`DROP TABLE`; `drop-database` for `DROP DATABASE` and `DROP SCHEMA` (`DROP
SCHEMA public CASCADE` is the reset that takes every table with it, so it
stands beside the rail's four shapes); `truncate` for a `TRUNCATE` that a
table follows — `TABLE`/`ONLY`, a bare or quoted name, a `format()` `%I`, or
the `' ||` of dynamic SQL — and not for one that `ON`, `OR` or a comma follows,
which is a trigger event (`BEFORE TRUNCATE ON t`), a privilege (`GRANT TRUNCATE
ON`) or a list, nor for the value `TG_OP = 'TRUNCATE'`; `unqualified-delete`
for a `DELETE FROM` whose statement carries no `WHERE`, the statement read to
its `;` or to the `)` that closes the CTE or `format()` call it sits in,
parentheses opened inside it balanced. String literals are read, as check 12
reads them, because `EXECUTE 'TRUNCATE ' || quote_ident(t)` runs the truncate;
a `--` comment quoting a statement is not a hit, so a header may say why the
file has none. Line numbers are the source's, through a plpgsql body.

Check 20 in `scripts/check-fork-consistency.ts` runs sixteen probes and
twenty-four non-probes through the rules every run — the tree's own shapes:
046's trigger, `AFTER INSERT OR DELETE OR TRUNCATE ON`, `GRANT … TRUNCATE ON`,
034's `DELETE FROM query_log` with the `WHERE` on the next line, 016's `DELETE
FROM _rte_in WHERE true` and its `USING` form, a CTE delete, a `DROP TRIGGER`,
a `DROP CONSTRAINT`, `ON DELETE CASCADE`, a `FOR DELETE` policy, a column
named `truncated_at`, 034's function comment — and a line-number probe with a
`TRUNCATE` on line 5 of a function body; then every `.sql` the tree holds
(84 today; check 15's skipped directories left out; a walk that finds none
fails in words), with counted per-(file, rule) exceptions as check 7 counts
them. The list is empty. `CLAUDE.md`'s rail and `CONTRIBUTING.md`'s checklist
item 5 say the rule in one sentence — a SQL file must never destroy existing
rows — name the check, and say the refusing trigger is the rule applied.

**Why.** Both documents said "no `DROP TABLE`, `DROP DATABASE`, `TRUNCATE`,
or unqualified `DELETE FROM` in SQL files" and nothing on this fork checked
it. Upstream's PR gate (`ob1-gate-v2.yml`, rule 5) greps the PR's changed
`.sql` files for the words on any line, comments included, and calls a
`DELETE` unqualified when its own line has no `WHERE`; the fork does not run
that gate (FORK.md's detach note), and the checker's SQL rules — 5 (a
`thoughts` column), 7 (an owned function), 12 (a Supabase-ism) — did not cover
these shapes. Read literally, the sentence fails the one file that applies
it: SMD-1730's review flagged 046's `BEFORE TRUNCATE ON thought_audit`
trigger, which refuses truncation, twice against the words, and the rail was
reworded on 2026-09-22 to say a refusing trigger is the rule applied. A grep
would still flag that file, and 034's two-line `DELETE`. So the rule is
stated as what it means and read as statements: what a file *runs*, not what
it says. The tree today: no `.sql` drops a table or a database; the migrations
`DROP FUNCTION` (16) and `DROP TRIGGER` (9), which the rail does not name and
which destroy no row; twenty-nine `DELETE FROM` statements, every one with a
`WHERE`; one `TRUNCATE`, the trigger event. The check passes the tree on its
first run, in the same 0.9 s.

**Scope.** Every `.sql` — the seven category directories, `docs/`, `deploy/`
and `db/migrations/`. The ticket asked whether a `DROP TABLE` in a migration
is ever legitimate here: no. A scratch table is 016's `CREATE TEMP TABLE … ON
COMMIT DROP`, the bench corpus and the test tables live in TypeScript, and
the one migration statement that once dropped a table (the dead `DROP TABLE
IF EXISTS` of a temp name the record of 021's backfill mentions) is gone. So
the migrations take the same rule with no carve-out. Outside the rule by its
own words ("in SQL files"): SQL inside `.ts` (compat's suite drops the tables
it makes; test-schema empties the one it owns) and the heredocs of a recipe's
init `.sh`. `test-schema` does not repeat the scan: the migrations are in
scope as files, and no substituted value is one of these statements.

**Held.** Check 20's probes fail by name when a rule loosens; the tree scan
fails by file and line. Planted in a recipe's `.sql`: `TRUNCATE thoughts;`
fails at its line; the same words in a `--` comment pass; a `BEFORE TRUNCATE
ON` trigger passes; `DELETE FROM thoughts;` fails; `DELETE FROM thoughts` with
`WHERE id = $1` on the next line passes; a `DROP TABLE IF EXISTS` planted in
migration 046 fails. Mutants, each reverted: `ON|OR` cut from the `TRUNCATE`
lookahead (the trigger-event exemption gone) fails 046:653 by name and three
non-probes with it; the statement no longer stopping at a closing `)` fails
the CTE probe; comments no longer stripped fails five comment lines of 036 and
046 and two probes; the `WHERE` test dropped fails twenty-nine statements of
the tree and the qualified probe; string literals blanked fails the three
dynamic-SQL probes; the walk matching no file fails in words; check 20 not
called passes a planted `TRUNCATE` — the call is the check.

**Not taken.** Blanking string literals, so prose in a `COMMENT ON … IS '…'`
or a `RAISE` message never trips a rule: an `EXECUTE` string runs, and
check 12 already reads literals for the same reason; the remedy for prose is
the same as 12's, a `--` comment. A `DELETE FROM … WHERE true` refused as
unqualified in spirit: 016's is on a TEMP table the function itself made, and
"has a WHERE" is a rule a contributor can read; the message names the case.
`ALTER TABLE … DROP COLUMN` on any table, which destroys a column's values:
the rail names four shapes, and check 5 holds the one table whose columns a
contribution must not touch; widening is a decision, not a check. Applying
the rules from inside `test-schema` as [10] does for the Supabase-isms: the
migrations are files in check 20's scope and gain nothing from a second scan
of their substituted text. A per-PR gate step: the checker runs on every push
over the whole tree, which is stricter than upstream's changed-files grep.

**Upstream status:** not sent — upstream keeps its grep in the gate, and the
statement reading exists to pass files upstream's gate has no equivalent of.
