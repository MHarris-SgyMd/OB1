---
type: fixed
bump: patch
tickets: [SMD-2083]
migrations: []
---

## Changelog

`integrations/rest-api`'s paging parameters (`limit`, `offset`, `page`, `per_page`) are each held to a finite integer in range before they reach SQL, so `GET /recent?offset=Infinity` (which rendered `LIMIT NaN OFFSET Infinity` and answered 500), a fractional `limit`, and a `page` past int4 no longer 500 — they clamp. A JSON body that is `null` (or any non-object) is a named 400 on every JSON route, where before a valid-JSON `null` was a 500 (SMD-2083).

## FORK

rest-api holds every paging parameter to a finite integer, and a non-object body to a 400 (SMD-2083)

**What changed.** `integrations/rest-api/index.ts`: one `intParam(value, { min,
max, default })` — `Number.isFinite`, `Math.trunc`, clamp; a missing, empty or
non-numeric value is the default, a non-finite one (Infinity, NaN, 1e400) is the
default and never rendered into SQL — replaces the `Math.min(Math.max(Number(v)
|| d, lo), hi)` idiom at every `limit`, `offset`, `page`, `per_page` and the
`/count` days window across the routes. `page` is bounded and, on `/search`'s
text path, the offset is clamped so `offset + limit ≤ INT4_MAX` — the invariant
that keeps `search_thoughts_text`'s `p_limit + p_offset` int4 addition from
overflowing (review pass 2), not merely `offset ≤ INT4_MAX`. The four JSON body
routes (`/search`, `/capture`, `PUT /thought/:id`,
`/duplicates/resolve`) now read through the existing `readJsonWithCap`, which
caps the body and refuses a non-object with `Body must be a JSON object` (400) —
so a `null` body is a named 400, not a `TypeError` the catch mistook for a 500.

`compat/supabase-sql/index.ts`: `limit()` and `range()` refuse a non-integer
argument with a named `RangeError` before any SQL is built (`requireIntArg`). It
is the backstop — a well-behaved caller clamps first — but it turns a generic
query failure into a legible one at the call site, since the compiler renders
`LIMIT`/`OFFSET` by interpolation, not a bound parameter.

**Why.** Found by SMD-2054's fourth review pass, adversarial, of the rest-api
file; all pre-existing, none a leak. `handleRecent` parsed `offset` as
`Math.max(Number(v) || 0, 0)`, so `offset=Infinity` reached the shim as
`offsetN = Infinity`, `limitN = NaN`, and Postgres saw `LIMIT NaN OFFSET
Infinity` — read as column names — and the route answered a generic 500. A
fractional `limit` survived the clamp and bound to an `INTEGER` argument; a `page`
past int4 refused at `p_offset INTEGER`; and `await req.json()` yielding `null`
threw a `TypeError` the `instanceof SyntaxError` catch did not cover.

**Held.** `bun extensions/test-writes.ts` against real Postgres (the rest-api
block), 389 assertions (from 381): `GET /recent?offset=Infinity` clamps to 0,
`?limit=2.5` truncates to 2, no `limit` and `?limit=` use the default 20 (not the
min), `POST /search` text mode `page=1e9` and `limit=100` at the max page each
answer a page (not an int4 overflow — the second exercises the `p_limit +
p_offset` boundary), `limit=2.5` reports an integer `per_page`, and a JSON `null`
body is a `400 Body must be a JSON object` — each a 500 on the pre-fix file.
`bun compat/supabase-sql/test-compat.ts` against real Postgres, 259 assertions:
`limit(2.5)` and `range(0, Infinity)` throw a named `RangeError` before SQL;
typecheck clean. The shim's other consumers hold —
`server-portable/test-store-postgrest.ts` 124 and `extensions/test-tools.ts` 185
against real Postgres.

## Review passes

| Pass | Finding | Caught by | Fix |
| --- | --- | --- | --- |
| 3 | No correctness or safety defect — the confirming pass. The cold read traced all 8 `supabase.rpc(` callers and their function bodies: `search_thoughts_text` is the only one with an int4 sum on a paging arg (`p_limit + p_offset`, now covered), semantic `match_thoughts` takes `match_count` and no offset, and the rest have no offset sum; `/thoughts` renders raw bigint `OFFSET`. The run-it pass killed the revert (`INT4_MAX - limit` → `INT4_MAX`) and an off-by-one (`→ INT4_MAX - 1`) mutant — so the arm pins the limit-dependent bound, not a constant — and probed `/thoughts`, semantic `/search`, `/duplicates` and `/entities` at extreme paging: `/thoughts` and semantic are 200; `/duplicates` and `/entities` 500 at offset=0 too (a dead route whose `find_near_duplicates` is undefined on the fork, and a fixture missing the entities table) — pre-existing, not paging | cold-read + run-it + mutant | none needed — the change held; the two survivors probed are non-paging pre-existing conditions, recorded below |
| 2 | A REAL residual defect in the exact class the ticket closes: `/search`'s text path clamped `offset ≤ INT4_MAX`, but `search_thoughts_text` computes `p_limit + p_offset` (int4), so `limit=100` at the max page gives `offset = 2147483600` (under INT4_MAX, so the belt never fired) and `100 + 2147483600` overflows → 500. Pass 1's `page=1e9` arm missed it — it used the default limit (25), so the sum stayed under. Found by the run-it pass tracing why a "page max unbounded" mutant died. The cold read confirmed all 13 paging call sites' `{min,max,default}` match the pre-fix literals exactly, and flagged a fragment wording nit (13 call sites, not 14 — the 14th `intParam` match is the definition) | run-it + mutant | the offset is clamped to `INT4_MAX - limit` so `offset + limit ≤ INT4_MAX`; a new arm sends `mode:text, limit:100` at the max page and asserts a page, not a 500 (verified: it fails on the old clamp) |
| 1 | No correctness or safety defect. The cold read verified all 13 paging call sites converted (no remaining `Number(...)` for paging), the `intParam` coercions (`"2.5"`→2, `"Infinity"`/`{}`→default, `[5]`→5) each yield a finite clamped integer or the default, the `page` bound is int4-safe in both handlers with no off-by-one, the empty-body change is a field 400 not a 500, and the shim `RangeError` breaks no consumer. The run-it pass killed 4 of 6 mutants; the two survivors were a real-but-minor coverage gap (the default for a missing or empty query paging param was unpinned) and the shim backstop (defense-in-depth — rest-api clamps before the shim, so it is never reached on the happy path). Mutating the `page` max to unbounded was KILLED — the bound is load-bearing (`search_thoughts_text` overflows int4 on `offset+limit`), not redundant with the offset clamp | cold-read + run-it + mutant | teeth added: a `GET /recent` with no `limit` and with `?limit=` each pin the default 20; a direct shim test drives `limit(2.5)`/`range(0, Infinity)` to the named `RangeError`. No code change |

## Not taken

`intParam` on the non-paging numeric fields (`min_similarity`, `importance`,
`threshold`): they are floats bound to non-INTEGER arguments and already clamp
finite, outside this ticket's paging scope. `/thoughts`'s `importance_min` is a
raw `Number(...)` bound on an INTEGER column, so a non-finite value there is a
bound-parameter error — an `{ error }`/500 carrying the value, not the
`LIMIT`-interpolation bug this ticket fixes (review pass 2); a legitimate
follow-up, out of the paging scope. Clamping `/duplicates`'s offset to
`INT4_MAX - limit` like `/search`: its `find_near_duplicates` is undefined on the
fork, so the route 500s before any SQL regardless (review pass 3) — an untestable,
no-teeth change today; a note for whoever adds that function, which would want the
same `p_limit + p_offset` guard. Rejecting an out-of-range `page` by
name rather than clamping: the other paging parameters clamp, so `page` clamps
too, for one shape across the routes. The `/ingest` `job_id` (`Number(jobId)`):
it is an id validated by the smart-ingest server downstream (a non-number is its
400), not a paging parameter this ticket holds. A `400` on `handleCapture` for an
empty `content` (as `PUT` has): pre-existing, out of this ticket's scope, and not
a 500 — an empty capture is a degenerate write, not a crash (review pass 1 nit).

## Boyscout

After the stop signal, one cut-for-space tidy in the touched test file, no
behaviour change (test-writes holds at 389). The four `GET /recent` paging arms
each repeated `send` + `assert(status === 200 && json[field] === want)`; they fold
into one data-driven loop over `[path, field, want, note]` rows, the shape the
CORS block just below already uses. The `intParam` conversion in `index.ts` is
itself the change's main dedup (one helper replacing the `Math.min(Math.max(Number(v)
|| d, …))` idiom at 13 sites), so nothing there was left to cut; the varied
`/search` POST arms stay as they are.

## Upstream status

Not upstream.
