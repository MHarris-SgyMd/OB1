---
type: changed
bump: patch
tickets: [SMD-1960]
migrations: []
---

## Changelog

Entity extraction streams every budgeted call's answer and aborts the call the moment the answer holds three copies of one item — the loop the runaways were measured to be, visible on the stream long before the answer budget — then retries under the penalty as a cut call is; on the fork's 32 stragglers a runaway is aborted a median 21 s into its call where it ran to its budget, the pass measured 1,796 s against 3,158 with a median 50 s per thought against 108 and 31 of 32 extracting — the 32nd extracts on a re-run under the shipped retry rule, so 32 of 32 is derived, not re-measured whole — and an answer that enumerates distinct ids is not a loop by the rule and still runs to the budget, which stays the bound; the penalised retry is read whole, since a penalised answer was measured to repeat an item three times and recover (SMD-1960).

## FORK

A runaway extraction call ran to its answer budget before it was known to be one — the answer is streamed now and the call aborted at the third copy of one item, then retried as before (SMD-1960)

**What changed.** `server-portable/entities.ts`: `extractOnce` requests
`stream: true` when the windowing says so (`ExtractWindowing.streamAbort`,
on for every budgeted call — `windowingFor` follows the budget, as the retry
does, since only a budgeted call has a retry to send an aborted one to), reads
the `text/event-stream` body frame by frame (`readStreamedAnswer`: `data:`
frames' `delta.content` reassembled, the last `finish_reason` kept, `[DONE]`
and comments passed over, frames split across reads buffered to their
newline; the body sniffed — one opening with `{` is a whole answer, read as
before), and feeds every piece to a `RunawayDetector`, which finds each
complete item — an object directly inside an array, at any depth, by a
container stack kept outside strings — and keys it as `parseExtraction` keys
it, (type, name) or (relation, from, to) in their own key spaces, the type and
relation UNVALIDATED (a repeated item the rules would reject is a loop all the
same; three tails looped on `table`, `document`, `issue`). At the
`RUNAWAY_REPEATS`-th (3) copy of one key the abort is pending; the next item
after it IN THE SAME ARRAY — the loop going on — is the abort: the reader is
cancelled (the connection closes, Ollama stops generating) and the call
returns malformed, a runaway, with `abortedMs`. An answer that closes or
finishes after its third copy, or goes on in its other array, is complete,
parsed, the copies folded — the same verdict however the frames split. `[DONE]`, a `finish_reason` or
the closing brace ends the read; a stream ending with none of them is thrown
as a closed socket unless what arrived is whole JSON. `extractCall` retries it under the penalty as it retries a
call cut at the budget — the retry read WHOLE, never aborted: a penalised
answer was measured to repeat an item three times and recover, and the
retry is the last attempt. A provider that answers a
stream request with plain JSON is read whole, as one not asked — every stub in
the suites does. `max_tokens` stays on every call: the detector is the early
exit, the budget the bound, and an answer that enumerates distinct ids (12 of
the 31 captured tails: every ticket a `uses` edge, tickets counted down) is not
a loop by the rule and runs to the budget as before. `Extraction` and its
per-window record carry `abortedMs` (the longest call's); the worker's dump
line carries it, its summary counts thoughts aborted on the stream, and
`describeExtractWindow` — the banner and preflight's `extraction window` row
— names the abort beside the retry. `EXTRACT_STREAM_ABORT` (`db/config.mjs`,
on, not a knob, as the retry is not) reaches `EmbedConfig.extractStreamAbort`.
`evals/eval-extract-windows.ts` gains the `whole+s`, `w1200s` and `w600s` arms
and an `aborted (median s)` column; `evals/eval-entities.ts` gains
`--unstreamed` for the parity run. No migration, no knob, no compose change.

**Why.** SMD-1879 measured the failures as generation that does not converge
— one entity or relation repeated to the context's end — and bounded each
call with an answer budget (3× the text's estimated tokens + 1,536) and one
penalised retry. A runaway therefore cost the whole budget before it was
known to be one: about a minute on `qwen2.5:7b`, and the retry only started
then (median 106 s per straggler, 20 of 32 retried). On a stream the loop is
visible as it happens. Streaming does not fix the failure — the model loops
whatever the transport; the budget and the penalty are what rescue — it makes
the failure cheap. The rule was chosen against the 31 runaway tails the
SMD-1879 probe captured: 16 repeat one item, 3 alternate two (a third copy of
either), 12 enumerate distinct ids and must not trip it; two copies is one
duplicate, which a converging answer does hold and `parseExtraction` folds.

**Held.** `test-thoughts.ts` [8c]: the detector over a converging answer with
braces and escaped quotes inside a name, fed whole, seven characters or one at
a time (never fires; every item counted); two copies (quiet); the third copy
fires on the item that made it three, keyed as an entity and as a relation;
two items alternating fire at the fifth; copies differing in confidence,
aliases, case or whitespace are copies; a rejected type repeated is a loop;
forty distinct ids as entities and edges are an enumeration (quiet); an item
that is not JSON is skipped; a nested object is its item's. The shipped
windowing streams and the sentence says so; reasoning on streams nothing; the
merge carries the longest window's `abortedMs` and none when none.
`test-local-provider.ts` [10]: a stub that streams a loop one item per frame
and records, per request, how many frames it got out before the client hung
up — the runaway is aborted after the third copy and before the loop runs out,
the retry carries the penalty and is read WHOLE (no stream requested), the
answer says how far in; a streamed answer whose frames split
tokens mid-name and mid-brace parses to the SAME `Extraction` as the whole
one; plain JSON to a stream request is read whole; a stream still coming at
the per-call deadline is the `TimeoutError` the worker classifies, counted as
one call; a stream that ends with neither a `finish_reason` nor `[DONE]`
throws a socket error naming what arrived, not a malformed answer.
`test-preflight.ts` [3c]: the row names the abort and the retry.
Mutant (scratch copy): the detector's firing line dropped fails two named
assertions — the stub sends all 42 frames and nothing says when it was aborted.

**Measured after.** `evals/eval-extract-windows.ts`, the 32 stragglers,
`qwen2.5:7b`, sequential on an idle Ollama, paired arms on the same day:
shipped `w1200p` 32/32, median 107.7 s, 3,158 s in all, 20 retried;
streamed `w1200s` 31/32, median 49.6 s, 1,796 s, the same 20 retried, 19
aborted 10.9–36.1 s into the call (median 20.9; the 20th is an enumerating
tail and ran to its budget as designed) — none of the 12 converging thoughts
touched, their times within a second. The one lost (`155e31a1`) fired the
detector on its penalised retry too, deterministically over three probes,
where the shipped arm's retry converged: a penalised answer can repeat an
item three times and recover, so the retry is read whole, and the thought
re-run so extracts twice of twice at 42 s against 122. Parity on the
fourteen labelled captures: streamed and `--unstreamed` identical — tp 21,
fp 10, fn 4, forbidden 2, rejected 10. Tables: evals/README.md, "The stream
abort".

**Review passes.** A cold read plus the detector mutant, then `/code-review`
at high effort six times. Five of pass 3's eight findings sat on pass 2's
fixes — the stop signal; passes 4–9, at the user's call, PROBED the reader
and the detector with stubs and found what reading had not.

| Pass | Finding | Caught | Fix |
| --- | --- | --- | --- |
| 1 | a stream that ended with neither a `finish_reason` nor `[DONE]` — the provider died mid-answer — came back as the model's malformed answer, where the whole read's `r.json()` on a truncated body threw and the worker classified it | cold read | thrown as a socket error naming what arrived; the worker's transient rule reads it |
| 1 | a thought aborted on the stream whose retry did not converge was recorded failed as "not JSON of the expected shape", the abort invisible to anyone sorting the failed rows | cold read | the failed row's error names the abort and how far in |
| 1 | `stream: true` has no off switch on a gateway that rejects it or streams ndjson | cold read | SMD-1973's scope (a comment there): the same standing as `max_tokens` and the penalty |
| 2 | a provider's error frame mid-stream (`data: {"error": …}`) was passed over and the stream then thrown as a closed socket — the cause lost, the row retried as transient | cold read | thrown with the provider's message |
| 2 | the failed row's abort clause read the thought's `abortedMs`, the longest of ALL its windows — one the retry may have rescued — so a windowed thought's failure was misattributed | cold read | the malformed window's own abort, or none |
| 3 | the error frame's throw carried no status, so a runner's 500 mid-stream was a per-row failure where the whole read's `r.status` paused and retried; the reader was left open on that throw until the deadline | cold read | the frame's code is the status; the reader is cancelled on the way out |
| 3 | the failed row's note claimed a retry whether or not one was made; an item with `from` but no string `to` was keyed as an entity by its `name`, which the parser never does; the stub's loop had 200 ms of margin | cold read | gated on `retried`; a relation or nothing; a second of margin |
| 4 | `[DONE]` only marked the end; the read waited for the socket, so a gateway sending keepalives after it held the call to its deadline and a good answer was recorded as this thought's timeout | cold read | `[DONE]` cancels the reader and returns |
| 4 | `"error": null` beside every choice threw on the first token; a numeric-string code carried no status; the failed row's note named the first call as the aborted one when the retry was | cold read | null is no error; a string of three digits is the code; the note names neither call |
| 4 | the sentence's alternatives for flags the config always sets together; `itemKey`'s field rules parallel the parser's (third raise) | cold read | declined: two separately flippable constants must each be told the truth; the shared pieces are shared |
| 5 | a frame carrying `finish_reason` marked the end but the read waited for `[DONE]` or the socket — a provider holding the connection after a finished answer made it this thought's timeout (probed); a whole answer arriving in one frame with a third copy in it was thrown away as a runaway instead of parsed (probed) | cold read | a finishing frame ends the read and is never an abort — the answer is complete and the parser folds the copies |
| 5 | a stray quote in a preamble before the JSON flipped the detector into a string for the rest of the answer (probed); two summaries said "the same thoughts extract" of a derived 32/32 | probe | no string state outside a container; "derived, not re-measured whole" |
| 9 | a finishing frame's content was never fed, so the same bytes were aborted when the finish came alone and parsed when it rode on the last content frame, and chatter after the brace in that frame was never cut (probed); the empty-answer rule counted frames, so the OpenAI role frame turned an empty answer into a malformed one (probed); items after a closed answer were still counted, so a second looping object in the same frame aborted a good answer (probed); the re-root took ANY object in an array inside an item, discarding items with nested object arrays (probed); a re-rooted answer never closed (probed); a valid empty answer never closed (probed); a CRLF split across reads dispatched half an event (probed); an unbalanced bracket AND a stray quote silence the detector | probe | every piece fed, the verdict from state in one order; no content = empty; nothing read after the close; re-root only out of an item that began as the answer, whose close is the answer's; an array opened in the answer closes it; the trailing `\r` held back; declined — the budget bounds |
| 8 | the fire was final, so a loop in a LATER array (a folded entity triplet, then relations looping) never armed the detector (probed); an unbalanced `[` in a preamble made the whole answer one "item" (probed); a runaway that arrived whole in one frame was accepted where the same bytes split were aborted (probed); the answer's last `}` and a second object's `{` in one frame lost the close (probed); an event's payload on two `data:` lines was dropped (probed); `[DONE]` alone read as a malformed answer where an empty body threw (probed); the failed row's note still said "a third time" | probe | armed anew when the array closes; the reading re-roots to the objects inside; the runaway before the close; closed is final and cuts the frame at the brace; events, not lines; one empty-answer wording; "the answer went on past a third copy" |
| 7 | the abort fired on ANY item after the third copy, so the same converging answer was complete in one frame and aborted when split (probed); a preamble's own `[…]` closing read as the answer closing (probed); `finish_reason: ""` on every frame ended the read (probed); a lone `\r` line end was never framed (probed); a 200 with no body was thrown as a closed socket, transient | probe | the abort is scoped to the array that holds the copies; `closed` needs an item read; only a non-empty finish ends; `\r` normalised; an empty body is the row's empty answer |
| 6 | a whole answer whose stream closed cleanly with no end sign was thrown as a closed socket — transient, so a provider omitting the marker stalled the pass row by row (probed); an SSE body under `application/json` fell to `r.json()` (probed); Ollama's finish comes in a frame of its own, so a converging answer holding a third copy was still aborted (probed); an unbalanced brace in a preamble hid every item (probed); one key space held entities and relations | probe | whole JSON at a clean close is the answer; the body is sniffed; an item is an object inside an array and a closing answer is complete — the abort waits for the item AFTER the third copy; `e:`/`r:` key spaces |

**Not taken.** Streaming with reasoning on (no budget, so no retry to send an
aborted call to; the answer's deltas would sit beside the thinking's); the
same treatment for `providerCall`'s chat path and `judgePair` (neither was
measured to run away — the ticket's condition); a rule that catches the
enumerating runaways (distinct items, no repetition: only a bound on the
answer's length would, and the budget is that bound); a knob for the abort
(one rule for the worker and the eval); a smaller `RUNAWAY_REPEATS` (two
copies is a duplicate a converging answer holds).

**Follow-ups.** SMD-1962: preflight's probes and the evals' diallers still run
under Bun's idle cut; the stream here resets it per frame, which is the third
outcome for that ticket's one `providerFetch`. SMD-2000: a call aborted on the
stream is the cheapest possible signal to escalate to the larger model on.

**Upstream status:** not sent — upstream has no extraction worker.
