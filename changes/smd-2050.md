---
type: added
bump: minor        # new preflight rows (jev tier, jev egress, jev decision) and an opt-in compose service
tickets: [SMD-2050]
migrations: []
---

## Changelog

A typed-decision tier for the Jev spikes: `jev/serve.ts` serves Verdict v1.4 (a 151M calibrated decision model) on onnxruntime under Bun behind the `ob1-jev/1` contract, `server-portable/jev.ts` calls it through the egress gate (`OB1_JEV_BASE_URL`, `OB1_JEV_MODEL`, `OB1_JEV_LOCAL`), preflight checks it when configured, and `--profile jev` runs it beside the stack (SMD-2050).

## FORK

A typed-decision tier the Jev spikes share — one serving process, one client, one contract; the seven spikes stop each carrying a runtime (SMD-2050)

**What changed.** Four pieces, none on the server's request path:

- `server-portable/jev-contract.ts` — the wire contract, `ob1-jev/1`, with no
  imports: `GET /health`, `GET /info` (the model's name, source, revision,
  weights and calibrator sha256, the limits), `POST /decide` (up to 64
  `binary` or `choice` decisions → per-option probabilities with the tier's own
  `__insufficient_evidence__` option, `abstained`, binary `p_true`, the raw
  logits and the temperature). `jevRequestProblem` is the one validation rule
  both sides read.
- `jev/` — the serving process: `serve.ts` (routes, one request at a time,
  loopback by default), `verdict.ts` (Verdict v1.4 = `heman10x/rlcd-modernbert-151m`
  at a pinned revision, its reference engine's prompt, per-K temperature and
  `[SEP]`-keeping truncation ported and held to its strings),
  `fetch-model.ts` (fetch the pinned bytes to a part of the fetch's own, rename
  only after the sha256 matches, verify every file on every start), `Dockerfile` (Debian slim:
  onnxruntime-node has no musl build; no weights in the image). A new
  type-checked directory (check 18) and a `FRAGMENT_DIRS` entry (check-landing).
- `server-portable/jev.ts` — the client: `resolveJevConfig` (null when
  `OB1_JEV_BASE_URL` is unset), `jevDecide` (the ticket's `(question, span,
  context)`, with the span written into the proposition by the caller),
  `jevChoose`, `jevDecideMany` (packs requests under 64 decisions and 8 MB,
  validates every decision before sending any), `jevInfo`. Every decision is
  judged alone by `mayLeaveBox` against an endpoint `OB1_JEV_LOCAL` declares
  (`LocalKnob` gains it; the subject kind `decision`): under deny a `marker`
  allows only in that decision's context, under allow a deny term reads all it
  sends. Refused is kind `egress`, a deadline `timeout`, a status `http` (422:
  a decision the model cannot read), an answer outside the contract or from
  another model than `OB1_JEV_MODEL` `body`; a refused connection or the
  caller's abort is rethrown as it came, providerCall's rule.
- Preflight, compose, docs — `jev tier` dials `/info` on every run when the
  knob is set (a wrong or unreachable URL fails the start, not a spike's first
  call), `jev egress` reuses the endpoint rows' rule, `--deep` adds `jev
  decision` through the client and its gate. `deploy/compose.yaml`: service
  `jev` under `--profile jev`, volume `jev-models`, no port (the host-ports
  file publishes it on loopback, `PUBLISHES` lists it), `restart:
  on-failure:3`; the server forwards the three knobs with no fallback — a
  default would fail every stack without the profile. `.env.example`,
  `deploy/README.md` "The typed-decision tier", `jev/README.md`.

**Why.** Seven spikes (SMD-1874, 1897, 1937, 2016, 2017, 2018, 2049) each
assumed a Jev-class model already loaded; the loading was work-item 1 of a
No-priority eval spike. The models do not fit the runtime the fork has: a
non-generative classifier returning a calibrated probability is not the
chat/completions shape Ollama serves, and Ollama exposes no option logits.

**What the ticket named and what runs.** openJev-verdict-2.0 is not servable:
its Hugging Face repository answers 401, and GitHub's LFS batch API answers
404 "Object does not exist" for its weights' pointer (oid `2201b07c…`, 598,509,338
bytes) in the repository and all 41 forks (checked 2026-09-23). Verdict v1.4 is the same author's
public checkpoint, the JevBench one — no separate confidence head, so
abstention is the explicit insufficient-evidence option. SemIf is Python-only
and goes behind the same contract as a follow-up (SMD-2052).

**Held.** `jev/test-jev.ts`, 104 assertions with no model ([1] the contract
rule, [2]–[4] the Verdict port against the reference's strings, [5] the engine
over fakes and its refusals, [5b] the handler's statuses, caps and serial
queue, [6] the verified fetch against a stub hub, [7]–[8] the client against a
stub tier that counts requests), 117 with `JEV_TEST_MODEL_DIR` ([9] the demo
presets and the refusals on the real tokenizer; [10] the conformance run,
`jev/conformance.ts`: the author's per-row receipt for this bundle, 1,000 rows
fetched pinned, reproduced exactly — every predicted option, confidence within
6.4e-6, accuracy 95.0%, abstention 97.5% / 89.45%, ECE 0.0335, Brier 0.0785,
all the report's; the served prompt and calibrator held at their measured
93.1% and ECE 0.212 on the same rows; [11] JevBench's two published Verdict
rows reproduced task by task, 231/231 each, `jev/jevbench.ts`, and
`buildPrompt` held byte for byte to the v1.4 row's prompt on all 213 choice
and binary tasks). Thirty-two mutants, each killed by the assertion it names
(`test-jev.ts`'s header lists them). CI runs install, tsc and the suite in
the portable-server job; [9]–[11] skip there (no weights in CI).

**Measured after** (Apple M5 Pro; `jev/README.md` has the table). Host: load
0.8 s, 1,029 MB resident after load, one decision p50 18 / 30 / 62 / 146 ms at
60 / 126 / 258 / 512 tokens. Container (podman VM): first start fetched the
606 MB in 17–20 s, restart serves in ~1.2 s, 926–987 MB, the same probability
as the host, 25–34 ms a short decision and 379 ms at 512 tokens (a full batch
~25 s, hence the client's per-decision deadline). A byte flipped in the volume's weights was
named, re-fetched and verified at the next start. Preflight's rows read as
designed against a live tier in six configurations (declared, undeclared,
wrong model, refused port, unresolved `jev`, unset).

**Provenance (SMD-1731).** Every answer names the model, source revision,
weights sha256, calibrator sha256 and `rules` — the engine revision and a
fingerprint of the prompt, budget, cut and temperature rules computed at load,
since the same weights under another engine answer differently; a caller that
stores a probability stores those beside it. Nothing here holds lineage for a derived row — that table is
SMD-1731's.

**The framing depends on the domain** (`jev/README.md`, "Conformance"). Paired
at 512 tokens, the v1.4 `It is` labels lose to bare labels on the author's
banking receipt (95.5% → 93.1%, 25 vs 1, p < 0.0001 — mostly wrong abstention:
15 of the 23 in-scope losses, 230 abstentions against 214) and gain on JevBench's
open-domain public tasks (54.1% → 57.6%, 3 vs 11, p 0.057); the bundle's
calibrator leaves answers unchanged but ECE 0.212 against 0.034 in-domain.
Kept as the engine ships it; every result carries the logits to refit, and a
spike's own workload decides.

**Review passes.**

| Pass | Finding | Caught | Fix | Commit |
| --- | --- | --- | --- | --- |
| 1 | the model's markers in caller text moved every slot; labels past 512 tokens answered unread; the 2 MB cap under valid batches; a caller's content replaced the text at the gate | run-it | markers counted, 422; one derived 8 MB cap, packed by bytes; joined | 94bbb340 |
| 1 | `--profile jev` crashlooped the server during the fetch; abandoned requests ran; shared `.part`s; handlers after the fetch; HEAD | run-it | `depends_on` healthy, `required: false`; 499; unique parts; handlers first | 94bbb340 |
| 2 | one `#public` context let every decision leave; a separator at token 505–510 read nothing; an unbounded `id`; a failed download kept its part | run-it | per-decision gate; the head must survive a cut; `id` capped; parts removed | b16df59a |
| 3 | one name for weights under two prompt engines: provenance could not reproduce | cold read | `rules`, a fingerprint of the prompt rules | 692b34ca |
| 4–5 | after main's merge the tier row leaked userinfo and skipped SMD-1875's rules; the operator run: a flat 30 s deadline timed out a queued batch in the container | run-it | masked, resolved first; 30 s + 1 s a decision, mid-batch stop | b15f2078, b7a363cc |
| 6–7 | pass 5's remedies misread a not-yet-listening `jev`, a proxy, TLS; NO_PROXY exempted exact hosts only, not Bun's rule; `/info` checked by name only; the gate read unvalidated input | run-it | Bun's NO_PROXY rule for every probed row; `/info` checked whole; validate, then gate | d5e576c9, review pass 7 |

**Boyscout.** After the stop signal, in the files already open, no behaviour
changed: the engine's refusal rules in one function with the docblock that
states them (`prepareDecision`), the dead `SLOTS` constant gone, the receipt
arm's prompt spelled with the marker constants, four unused stub parameters.

**Not taken.** A Python sidecar for Verdict (onnxruntime-node under Bun runs
it, ids identical to transformers.js); transformers.js (the tokenizer alone is
360 KB, no dependencies); a padded batch (no faster than singles on this CPU);
a compose fallback for `OB1_JEV_BASE_URL`; the co-load measurement beside the
embedder and metadata model and the SMD-1937 consumer run — the second PR, by
the plan's cut.

**Follow-ups.** SMD-2052 — SemIf behind `ob1-jev/1` (a host adapter; Python, MLX).
SMD-2065 — `compose.tiers.yaml`'s server blocks do not forward the `OB1_JEV_*`
knobs (nor `OB1_EXTRACT_CHUNK_TOKENS`), and check 14 does not read the file.
SMD-2082 — the tier's queue is unbounded (a burst of large requests took it
from 0.9 to 2.1 GB resident); bound it by count and bytes, 503 past the bound.
