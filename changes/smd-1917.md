---
type: changed
bump: patch
tickets: [SMD-1917]
migrations: []
---

## Changelog

FORK.md is a 60 KB front door with a generated index; every numbered change (18–103) is one file under `changes/`, release fragments sit beside them until they are numbered, and check 15 holds the sizes, the numbering and every citation of a change number (SMD-1917).

## FORK

FORK.md is the front door, and every change is one file — `changes/NNN-<slug>.md` with a fixed shape and a 150-line cap, an index generated from the directory, fragments numbered into it at release, and check 15 holding the sizes, the numbering and every citation of a change number (SMD-1917)

**What changed.** The 85 `### N.` sections FORK.md carried for changes 18–102
(and 103, which reached `main` while this was in review) are one file each under
`changes/`, named `NNN-<slug>.md`, the heading level changed and the text
otherwise as it was at the split. FORK.md keeps the pin, the
version scheme, what the fork is for, the 1–17 table, "Files we own", the drift
guards and the standing sections, and carries a **generated index** — one row per
change, the title's first clause and its ticket, linked to the file, and the
fragments landed since the last release by ticket — between two marker comments
that `scripts/fork-index.mjs` rewrites from the directory, so the count and the
list are never kept by hand. A change file has a shape (`changes/README.md`: what
changed, why, held, measured, a review-pass *table*, not taken, follow-ups) and a
**150-line cap**, fragment or numbered. SMD-1804's fragments keep their shape and
their rule — a PR ships `changes/smd-NNNN.md`, the release step assigns the
number — and the release step now writes each fragment as the next numbered
*file* and regenerates the index, instead of appending a `### N.` section to
FORK.md; its FORK-count check is retired (the directory is contiguous by check
15), its fragment rule stops refusing a citation of an *existing* change number
(check 15's dangling rule holds every citation), and its release pairing reads
the tickets from the change files. Two placements moved at the split: the
"second vector store beside Postgres" design written for SMD-1038 sat inside
change 19's span and is change 79's pre-registered shape, so it is in 79's file
(its two citations in `evals/` follow it); "The recurring defect in this fork: a
value defined twice" is a house rule and is a standing section of FORK.md. Two
long paragraphs of the standing sections that were a change's narrative — the
vendored-content audit's four rules, the SMD-1728 window and attribution fixes —
are a paragraph each, pointing at the change files and the pass commits. Change
102 is the first record cut to the shape: 470 lines, eight review-pass
paragraphs, to 119 lines with a 22-row findings table; this record is the second.

**Why.** FORK.md was 1,237,711 bytes and 17,705 lines at `main` c6fc294 — about
300k tokens, larger than any context window in use, growing ~100 KB per merged
PR as every review pass appended a paragraph whose facts already sat in the
commit body's `(caught: …)` tags (change 100's tooling reads those from commits,
never from FORK.md). No reader took it whole: every agent that "read FORK.md"
took a slice by grep and paid for the rest by not knowing what was there, and a
reviewer verifying one record re-read a 457-line section on each of eight
passes. The one file also made the numbers hand-kept: every merge of `main`
renumbered a section — change 102 was 100 on its branch — and the 231 code
comments and docs citing "FORK.md change N" kept whatever number a section had
when they were written, wrong silently after a renumber. SMD-1804 ended the
hand numbering ten minutes after this branch's first commit; it still wrote
the record into FORK.md.

**Held.** Check 15, a pure `forkLayoutProblems({ entries, forkText, citations })`
over in-memory entries with layout probes and citation-reader probes on every
run: a file under `changes/` is `NNN-<slug>.md` or `smd-NNNN.md` (or the README);
the numbers run contiguously from 18 with no duplicate; the first line of a
numbered file is `# N. <title>` with the name's number; a file is at most 150
lines, the 47 over it when the split landed listed in `OVERSIZE_AT_SPLIT` with a ceiling
they may only shrink under, the list held stale two ways; FORK.md is under 64 KB,
carries no `### N.` section, and its index equals what `fork-index.mjs` renders —
the tools.json round-trip; and every citation of a change number in a tracked
text file — "FORK.md change N", "changes N and M", "FORK.md §N", a `changes/NNN`
path, `db/README.md`'s "NNN change M" map, and the bare "change N" the record
itself uses — names a number with a file or a row of the 1–17 table. Seventeen
mutants on the real files bite: an upper-case name; a second file numbered 44;
44 removed; 44's heading saying 45; 44 grown to 168 lines; 19 grown past its
ceiling; 19 shrunk under the cap with its entry left; a `### 104.` left in
FORK.md; one index row edited; FORK.md padded past the ceiling; `deploy/README.md`
citing 175 and `db/README.md`'s map citing 145, each reported at its line; the
dangling, duplicate and wrong-number branches each dropped (the probes name
them); the no-heading branch dropped (its probe throws instead of reporting —
the run still fails); the bare-citation reader dropped. The check's own first run
reported this record's citations of its own number before the file existed — the
dangling rule on the author, and the reason a fragment carries no number.
`assemble-release.mjs --self-check` holds the retargeted writer: a fragment's
file name and heading from its number and title, and the index re-rendered.

**Measured after.** FORK.md 61,543 bytes and 828 lines (SMD-1804's Versioning
section included) with 86 index rows and two fragments listed;
`changes/` 86 numbered files, two fragments and a README, 39 under the cap, 47
listed; the whole check script runs in 0.9 s. `bun scripts/fork-index.mjs` twice
leaves no diff. The record of one change, for a reviewer, is one file: under 4k
tokens for the two cut to the shape, the largest legacy file (28, 707 lines)
about 12k, against a grep into 1.2 MB before.

**Review passes.** A cold read beside a run-it reviewer with a mutation harness
on the real files and a throwaway copy of the tree, each pass; one row per
finding that changed the mechanism, the rest in the pass commits. Pass 2's top
findings were two crashes on a directory entry, pass 3's a dull tooth in 17b and
the reader the assembler kept for itself — not the stop signal yet.

| Pass | Finding | Caught | Fix |
| --- | --- | --- | --- |
| 1 | the citation reader was anchored on `FORK.md`; "FORK change 62" in migration 036 was stale (the residue is change 63) and unread | cold read | `FORK(?:\.md)?(?:'s)?` anchor, three probes; 036's comment left as it is (a migration's text is frozen by its ledger sha) — noted here |
| 1 | the bare reader took the first number of a list; "change 2026-09-21" read as 2026 | cold read | one `CITED_LIST` shape, `-NN-` date guard, four probes |
| 1 | the walker read ignored files (a planted citation in `.env`, an evals corpus, a recipe's data/ failed locally, not in CI) | run-it | `citationFiles()` asks the VCS; walk as the fallback |
| 1 | 17b read tickets from titles alone — a "(SMD-1301 / 1302)" title would fail its own release | cold read | the whole change file |
| 1 | check 16 let an unnumbered `### Title` or a wrapped title through; the writer would have produced `# 104. ### Title` | cold read | plain title line then blank; writer strips a mark; two probes |
| 1 | eight branches of checks 16 and 17b had no probe (deleting each passed) | mutant | one probe per branch, pairing probes fire one branch each |
| 1 | the release step wrote files and unlinked fragments before the index splice could throw | cold read | FORK.md rendered from the plan before any write |
| 1 | a fragment with no `## FORK` body crashed the dry run anonymously; no fragments gave a stack trace | both | named errors, a sentence and exit 1 |
| 1 | "a fragment" was named three ways (`/i` in the assembler) — `SMD-2003.md` refused by 15, numbered by the assembler | both | one `FRAGMENT` export |
| 1 | "Changes 1–102" in three docs and 1804's fragment still describing FORK.md sections | both | 103; the fragment says change files |
| 2 | a directory listed by the VCS (a nested repository such as an agent worktree under `.claude/`) or under `changes/` crashed the check with EISDIR | both | regular files only; a directory under `changes/` is a bad name |
| 2 | the release step still had two throw sites after the destructive writes (the changelog insert, the frozen shas) and numbered on top of a gapped directory | cold read | the plan renders everything; `write()` only writes; a non-contiguous directory is refused |
| 2 | a numbered file below 18 passed and joined the index beside the table | cold read | `below-first`, one probe |
| 2 | a `\|` or bracket in a title broke the index table; the check could not see it (writer and check render alike) | cold read | escaped in the cell |
| 2 | the continuation reader took "(FORK.md change 90, 1536 dims)" as citing 1536 | run-it | continuation numbers change-sized and no thousands group; two non-probes |
| 2 | an explicit citation in the record was reported twice (both readers matched the span) | run-it | the bare reader runs over the text with explicit spans blanked |
| 2 | check 16's numbered-heading rule fired on a `# 1. install` comment inside a fenced block and on `#1.` | run-it | fences stripped, a heading's one space; one non-probe |
| 2 | a fragment with a BOM or CRLF was "no front matter" | run-it | the parser strips both |
| 2 | `slugOf`'s budget applied only past three words, the stop-word trim only above three | cold read | budget from the first word, trim while one word remains; the names 18–103 stand |
| 2 | `changes/` was read three times per run | cold read | once, cached for checks 15, 16 and 17b |
| 3 | 17b's released tickets were seeded from all of FORK.md's prose, which names every pending ticket — a release that left a fragment unnumbered passed | cold read | the numbered files alone |
| 3 | the leading citation number had no thousands guard ("changes 1,536 rows" → 1 and 536); a bare comma continued a list into "(change 90, 250 ms)" | both | one `NUM` guard on every number; a comma continues only before another item or "and" |
| 3 | a `changes/NNN-<slug>.md` path was checked by number alone, so a renamed file left the link dead and the check green | cold read | a slugged path must name the file as it is |
| 3 | the assembler read fragments through its own reader: a symlink named like one was numbered, a pipe hung it; a stray or an empty directory passed; a fragment could list a frozen or missing migration | run-it | fragments from the one reader; strays, an empty directory and a migration outside the range refused in the plan |
| 3 | `# 104.` and `##### 104.` were not refused in FORK.md against the docs' "any level" | both | `#{1,6}` |
| 3 | a BOM before a change file's H1 read as "no heading"; " — " inside a code span split the index cell; an empty directory rendered "18–17" | cold read | stripped; split outside code spans; a sentence for none |
| 3 | the release date was the machine's UTC day | cold read | the house zone, America/Chicago |

**Not taken.** Rewriting the 231 "FORK.md change N" citations to `changes/NNN`
paths — the numbers stay the same and the index maps each to its file, so the
check that N has a file is the whole fix. Cutting the 84 legacy files to the
shape — the ratchet holds them where they are and lets each shrink when
touched. Enforcing the shape's paragraphs — a measurement-only change has no
"Held", and the cap is what stops the sink. `git mv`-style history for the
split: git does not follow one file into eighty-five, so the README says where
the history is. Numbering at PR time, as the ticket first proposed: a number a
PR takes can still collide with another PR's, and main's ruleset runs the checks
on the PR head, so the second lands and main goes red until a hand renumber —
SMD-1804's release-time numbering has no such case, so it stays the rule.

**Follow-ups.** SMD-1541 reached `main` as `### 103.` while this was in review —
the last hand-numbered change; it is `changes/103-…` with a ratchet entry, since
twelve code comments cite the number. SMD-1901, still in flight, ships its section
as a fragment instead — no number to pick. The house memory
rule "re-check main's numbering before merge and push" retires with this change
(SMD-1805's table, first row).

**Upstream status:** not sent — FORK.md and `changes/` are this fork's record;
upstream has neither.
