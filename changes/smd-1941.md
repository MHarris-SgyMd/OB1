---
type: changed
bump: patch
tickets: [SMD-1941]
migrations: []
---

## Changelog

The egress gate no longer judges a capture on its `source`: a capture's `source` is the caller's claim (`capture_thought` takes it, `mcp` when omitted), so a `source:` term that only a capture would carry could be dodged by naming another label or none. A `source:` term now gates a row's OWN stored label — at an edit and at the re-embed and consolidation passes that read the row back, where the value is the server's — and who may capture is gated by `actor:`, which the key proves (SMD-1941).

## FORK

A `source:` egress term gates a row's stored label, not who may capture (SMD-1941)

**What changed.** `server-portable/egress.ts`: `termMatches` refuses the `source`
unit for the `capture` kind — a capture's `source` is the caller's claim, so a
`source:` term cannot gate it. Every other kind (`edit`, `re-embed`, `judge`,
`extraction`, `decision`) still matches on `metadata.source`, the value the
server wrote on the row. The rule lives in the one gate function, so it holds at
each layer that calls it: the server's `decideCalls` at a capture, the
embedder's defense-in-depth `providerCall` re-gate, and the workers'
`refusesEverything`.

`server-portable/index.ts`: the capture `EgressSubject` no longer carries
`metadata: { source }` — nothing on the capture path is judged on it, so the
call site says so. The row still RECORDS `source` in its payload (unchanged), and
the edit path still judges the ROW's stored `source` (`kind: "edit"`), the
opposite of a capture: the `kind` tells the gate which the value is.

`server-portable/preflight.ts` and `deploy/.env.example`: the `source:` warning
and the unit docs say a `source:` term does not gate a capture (its source is a
claim) and gates a row's stored label at an edit and the passes; gate who may
capture with `actor:<key name>`.

**Why.** SMD-1298 gave `capture_thought` an optional `source` so a session-end
hook's summary lands with `metadata.source` naming the harness. Before it, every
MCP capture's `source` was the server's own constant `mcp`, so an egress term
like `deny source:claude-code` was trustworthy for captures — only the server
wrote the value. Once the caller names it, a hook (or whoever holds its key) that
omits `source` is `mcp` to the policy, and a term meant to keep hook captures off
a remote model is dodged by not naming oneself. A capture's `source` cannot be a
security boundary; `actor:` (the key, proven) is. The value keeps its worth as a
row's stored label — for the per-source weight (SMD-1297) and for the passes'
gate over rows already tagged — where the server, not the caller, wrote it. This
is option 2 of the ticket: make `source` advisory at capture, rather than binding
a label to the key record (option 1).

**Held.** `bun server-portable/test-egress.ts` against real Postgres, 122
assertions: a `source:mcp` allow term does not let a capture through and a
`source:mcp` deny term does not hold one back — even a capture subject that DOES
carry the label — while the same term gates a `re-embed`, `edit` and `judge` of a
row labelled `mcp`; `termMatches` returns false for the `source` unit on a
capture and true on the others. `bun server-portable/test-preflight.ts`, 175
assertions: the `source:` warning says the term does not gate a capture and
points at `actor:`. `bun server-portable/test-server.ts`, 285 assertions, and the
server typecheck: unchanged, the capture path still gates on `actor` and `marker`.

**Review passes.**

| Pass | Finding | Caught by | Fix |
| --- | --- | --- | --- |
| — | (pending) | | |

**Not taken.** Binding a `source` label to the key record (option 1: a fourth
`MCP_ACCESS_KEYS` field `name:scope:sha256:source`, `authenticate` putting it on
the `Principal`, `keygen --source`, the six `_shared/auth.ts` copies synced,
preflight listing it) — it would make a `source:` term trustworthy for captures,
but it enlarges the security-critical `auth.ts` and its six copies and the key
format for a label whose only proven form is already `actor:` (the key name); the
maintainer chose the smaller, honest fix.

**Follow-ups.** None filed. SMD-1724 (a trust label from the source carried into
every read) is the general form.

**Upstream status.** Not upstream.
