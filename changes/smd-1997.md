---
type: added
bump: patch
tickets: [SMD-1997]
migrations: []
---

## Changelog

The event-sourcing decision, recorded: `thought_audit` is designated the write-side source of truth for a thought and the `thoughts` row its projection — kept as a table with its DDL surface and its three write functions, so no vendored writer changes — with the event's shape, the three verbs as a read, every projection's snapshot key, the two clocks, deletion and forgetting, and a three-step additive path from 053 written down in `docs/event-log-as-truth.md` and the steps filed (SMD-1997).

## FORK

The event log is the source of truth — the ADR, decided on two measured gates: `thought_audit` is the write-side truth, the `thoughts` row its projection kept as a table, the write functions the projector, and a three-step additive path from 053 (SMD-1997)

**What changed.** `docs/event-log-as-truth.md` is the architecture decision record the ticket asked for: the decision in eight numbered points, what the schema already held at 053, what the two gates measured (SMD-1998's replay, SMD-1999's writable projection), the event's columns and where each came from with the three additions the decision makes, the three verbs (ingested, comprehended, expressed) as a read over the columns the event already carries with a precedence, the write path from step 2 on (append, project, the trigger as the check, the projector's three announcing settings cleared before it returns, the cascade rule, the vector refresh and the snapshot seeded once then fed by live writes, the five "no event, no write" deltas accepted, the log faithful from step 1 on), a table of every projection with the key it records today and what the decision requires of it, the two clocks and the two as-of reads, deletion and forgetting, the consistency rule (atomic at the boundary, eventual off the row, convergent by rebuild; idempotent by the check; conflict preserved not resolved), the path in three steps with what coexists and how each rolls back, what is declined and why, what is not decided here, what holds it, and the prior art. One sentence in `CLAUDE.md`'s guard rail, one paragraph in `db/README.md` beside migration 046 and one line in `evals/README.md`'s Related point at it.

**Why.** SMD-1997 asked whether to restructure the data model around event sourcing and set two spikes as gates. Both opened: SMD-1998 measured that a rebuild reuses every vector by `(content_fingerprint, embedding_model)` and that the graph, not the index, is the expensive projection; SMD-1999 measured that the three write functions can append the event first and project the row in the same transaction with an empty contributor delta, and that a view named `thoughts` breaks the community schemas' DDL. The ticket's Verify names four things a decided ADR produces — the record with the taxonomy, the projector and snapshot strategy and the bitemporal model; the two spike results; a migration and compatibility plan naming how the log as truth coexists with the mutable path — and this is the first and the fourth, the second and third cited where they stand.

**Decided.** The log is the truth for the thought row and the row is derived from its event in the same transaction; `thoughts` stays a table (Postgres refuses `ADD COLUMN`, `CREATE INDEX`, `REFERENCES` and a row trigger on a view, which the community surface runs, and `ON CONFLICT`, which 053's own capture runs); the functions are the projector and the audit trigger becomes the check under `ob1.projecting`, still appending for a raw write; read-your-writes by construction, no queue; the verbs are a read with a precedence — ingested by the trust ceiling the trigger enforces, then comprehended by a worker's door in `origin`, else expressed — and not a column, which would be a second declaration free to disagree with the columns that determine it; the precedence is fixed and its middle term waits on a door vocabulary that does not exist yet; comprehension on the row is an update event from a worker's door, comprehension off the row (entities, chunks, proposals, facets) is a projection with a lineage row under SMD-1731, not a second log; every projection's key names the input's fingerprint and the recipe or its rebuild cannot be incremental, the vector snapshotted by its key in a table seeded once from the rows and fed by live writes alone (the seed holds each thought's current text, so the fold calls no provider and reuse holds for every thought's final text, not the states between); transaction time is `created_at` + `seq` with `created_at` first (050's own rule for pre-050 rows), valid time the 046 window, the as-of read over the first the replay with a bound and over the second a filter SMD-2011 wires; deletion stays real, forgetting is the second named amendment of the log by `thought_id` over every row holding the text, a redacted thought projects to nothing by a marker kept beside the log, and the redaction lands no later than step 2 because from then the log holds every thought's text; the five deltas SMD-1999's record and bodies named against 053 are taken (no `updated_at` bump on an identical re-capture, a no-op edit or a vector refresh; the unreachable stale-read arm gone with the refusal kept and the function's COMMENT rewritten; the 2-argument capture's row lock) and said to be the one caller-visible change, beside one bound C6 did not measure: a community row trigger that writes a `thoughts` row under a projection meets the check.

**The path.** Three additive steps, filed as Phase 1d/1e/1f of SMD-1729 in the milestone with 1730–1732: SMD-2115 (the capture event carries the content and a backdating writer's `created_at`, the update event the key's move; 046's diff rule, its append and 050's stamp become functions; the payload backfilled onto earlier capture rows through a third arm in 046's immutability gate, or the replay seeds from the row store, decided there after measuring), SMD-2116 (the functions append then project, the trigger checks, the vector snapshot seeded and the refresh, the accepted deltas; blocked by SMD-1723's redaction as well), SMD-2117 (`db/fold.ts` — the fold in `(created_at, seq)` order, bounded, into a tier or a schema, with a verify mode, its input the log and the snapshot, rebuilding the thought rows alone and naming what it did not, defined over the events since step 1 until the backfill has run and faithful from step 1 on; the raw in-tree writers onto the projector; `thought_changes` reading a capture's head from the event). Each blocked by the one before. "Dual-write" is retired as a word: from step 2 there is one truth, and the period it named is the one in which raw writers still write the row first and are trigger-audited.

**Declined.** A view with `INSTEAD OF` triggers and a projection table behind a compat view (the DDL surface, measured); pure event sourcing with no snapshots (hours for the graph); an external bus or an asynchronous projector (Postgres-only, read-your-writes); a second log for comprehension; dual-write in application code (three functions are the one door); a verb column; a fourth `action` on 008's append-only table for the redaction marker; making the record itself a checked artifact (its numbers live in the two README sections and the migrations, each held by its own suite — a second copy here would be one more thing to hold stale). Cryptographic shredding declined for forgetting on a single-operator brain; revisited if the log ever leaves the operator's database.

**Held.** By nothing in the page. The two gates' runners stay in CI (the pure rules self-checked in the portable-server job; gate 1's fixture and the prototype applied to a real Postgres at 053 in the data-layer job, the latter held to a recorded matrix) until SMD-2116 ships the bodies and their criteria move into the schema suites; test-schema's sentinel reads and preflight's recognisers pin the write functions and move with them at each step; check 21 keeps every `.sql` from destroying a row the brain holds; the version rules cut each step as a MINOR fragment with its migration. The record's own claims are those sections' measured results, cited, not restated as new numbers.

**Not decided here.** The log's partitioning (046 chose `RANGE` by month; SMD-1947 benches and decides); the redaction amendment's exact shape (SMD-1723; what the projector does with a redacted thought, and where the marker lives, is decided); the graph's fingerprint and every recipe (SMD-1731); where a pre-step capture's text comes from on a replay (SMD-2115 measures the backfill first; until then the fold is defined over post-step-1 events); the door vocabulary `origin` holds — file paths, bare words, server names and one SQL function's name today — and a helper that reads the verb (filed when a reader needs it); a Zep/DMR benchmark (SMD-1039).

**Review passes.** Each an independent cold read at high effort beside a run-it reviewer that traced every number, name and mechanism to its source, one commit per pass.

| Pass | Finding | Caught | Fix |
| --- | --- | --- | --- |
| 1 | the gate-1 paragraph quoted an earlier, unpublished run — 419 thoughts, 1.75 M characters, 419 of 419, 7 minutes, 4.3 hours, 209 of 419, 1e-16, 33×, 4.4 MB — where the README section it cites publishes 440, 1.83 M, 440 of 440, 8.4 min (5.0 trimmed, 7.6 by characters), 4.5 h, 217 of 440, 2.2e-16 (1.1e-16 on the windows), thirty-two times and 4.3 MB; three of the wrong figures were ones SMD-1998's own pass 1 had retracted | run-it + cold read, both | the paragraph restated from the published block; "MB of text" is characters; the order-of-magnitude caveat carried |
| 1 | the projector was said to clear `ob1.event` alone, so a raw write later in the same transaction would meet the check and be refused — the promise "a raw write is audited, never refused" could not hold | cold read against `common.sql` | the three announcing settings named, cleared before the projector returns, `ob1.actor_amend` and `ob1.cited_delete` restored |
| 1 | the payload backfill was called "the third named amendment" without saying 046's gate refuses an UPDATE of `diff` under any setting and test-schema asserts so | cold read against 046 | the path's step-1 row names the third arm and the assertion that moves; SMD-2115 amended |
| 1 | forgetting and replay collided: a redaction blanks the capture's content and the projector refuses a content-less capture, so one redaction would stop every fold; and step 1 copies every thought's text into a log with no removal path | cold read | a redacted thought projects to nothing (the saying-so row is a tombstone for the fold, the count reported); SMD-1723 lands no later than step 2 and blocks SMD-2116; step 1's rollback column says the text is unremovable until then |
| 1 | the verbs table named a re-embed as an update event while the write path made it a refresh with no event; the example doors (`reembed`, `label`) exist nowhere — writers pass `db/reembed.ts`, `consolidate`, `ingest-records`; the read rested on `stance`, a declared column, while declining a verb column for being declared; two rows overlapped with no precedence | run-it (grep of every `via`) + cold read | the re-embed moved out of the event rows; the real doors quoted and the door vocabulary named a gap; a precedence (trust ceiling, then door, else expressed) with `stance` descriptive only |
| 1 | the fold was bounded by `seq` where 050's own comment says `seq` is exact only after 050; "nothing changes for a writer" where two stamp deltas are caller-visible; a fifth delta in the prototype's bodies (a no-op edit writes nothing) unlisted; the snapshot's feeding trigger unqualified, so a replay would rewrite `taken_at`; `created_at`'s fallback unsaid; `KNOWN_MODEL_DIMS` credited with the template hazard `EMBEDDING_PROMPTS` carries | cold read against 050, option2-functions.sql, common.sql | the bound is `(created_at, seq)`; the contract sentence names the surface and the stamp change; five deltas; the trigger records live writes only; the event row's own `created_at`, never the replay's clock; the hazard named |
| 1 | smaller: "three shapes prototyped" (two, across four schemas); the replay summary omitted the four tolerated NULL keys; the ×3 outlier dropped; `rebuild_derived` in the present tense; the data-layer fixture step missing from "Held by"; `dims` attributed to the prototype's snapshot; the `node_state` measure attributed to the wrong ticket and without its n; the guard-rail sentence in `CLAUDE.md` stating the projection as present fact | run-it + cold read | each corrected; the guard rail reads "will stay … no step has landed" |
| 2 | the vector snapshot was never seeded: fed by live writes from step 2, it holds no row for a thought not rewritten since, and gate 1 measured reuse against the vector on the `thoughts` row a fold wipes — so the first fold would re-embed the corpus the record says it reuses | cold read | a one-time seed at step 2 from every row holding key, model and vector; gate 1's number holds after it; SMD-2116 amended |
| 2 | the step-1 backfill fills the content and not the key's move, which pre-step update events never carried, so the faithful projector would rebuild every pre-step-edited thought with its capture-time key (288 content-moving updates on the dogfood log) and the verify mode would report hundreds of differences outside the one tolerated kind | cold read against `common.sql`'s faithful arm | faithfulness begins at step 1: for earlier update events the projector derives the key from the content and counts them; 018's NULL cannot be recovered after the fact; SMD-2115 and SMD-2117 amended |
| 2 | "a row trigger keeps working" was wider than C6 measured (it applied the DDL in a rolled-back transaction; it did not write under a projection): a community row trigger writing a `thoughts` row during a function-borne write meets the check and is refused unless it is a bump or a successor's pointer | cold read against C6's method | the contract bullet bounds the promise and says no schema in the tree does this |
| 2 | `EMBEDDING_PROMPTS` attributed to `embed.ts` — pass 1 had moved it from one wrong home to another; it lives in `db/config.mjs` beside `KNOWN_MODEL_DIMS`; the door list omitted `db/sync-linear.ts` (a production writer) and `backfill_thought_actors` (a SQL-only origin), the two writers under which the verb precedence reads wrong; `rebuild_derived` still present-tense in two places; the `ON CONFLICT` refusal credited to C6 (it was C1 and C2); `db/replay.ts` collides with `db/test-replay.ts`, the query-log replay gate; the lock enumeration was not the order it claimed | run-it | the file named; the doors added and the precedence marked not executable until the vocabulary lands; the tense; C1–C2 and C6; the fold tool is `db/fold.ts`; 036, 033, 032 |
| 2 | the redaction marker had no home the fold could read (008's CHECK admits three actions); "the snapshot travels with the log" named a step nobody owned; the fold's destruction of the off-row tiers, which gate 2 stated, went unsaid; delta 4 falsified 046's COMMENT on `update_thought` and nothing moved it; the trust rule was narrower than 046's ceiling; the width was placed on the row here and on the snapshot in SMD-2116; a tombstone had no verb | cold read against 008, 046, gate 2's report, SMD-2116 | a marker table beside the log, a fourth action declined; step 3 copies log and snapshot both; step 3 rebuilds the thought rows alone and names the rest; the COMMENT moves with the body; 046's rule quoted; `dims` beside the snapshot row; a tombstone carries no verb of its own |
| 3 | pass 2's seed did not make gate 1's number hold as written: the seed holds each thought's current text, a fold lands the capture's text first, so every edited thought misses at capture — and the prototype calls no provider on a miss (a NULL vector on a capture, the one before on an update) — the stop signal, pass 2's fix as the top finding | maintainer read against gate 1's method and the projector's replay arm | the rebuild column says the fold calls no provider; reuse holds for every thought's final text, not the states between; 015 fills what the snapshot lacks |
| 3 | pass 2's "the projector derives the key" for pre-step update events would write a held key onto a second row and raise on 003's partial unique index — 018 exists for that case — and the prototype keeps the row's key | maintainer read against 003 and 018 | the derivation follows 018's own rule (NULL when another live row holds it) and is SMD-2117's arm; "two things a replay fills"; the verify mode's two tolerated kinds named |
| 3 | pass 2's row-trigger bound was wider than the check: the nulled-pointer arm is gated on a tombstone's projection and an exactly-equal diff, and a trigger writing the event's own row meets the AFTER-image rule, not the foreign-row arm | maintainer read + run-it against the foreign-row arm | the contract bullet states the check's rule exactly |
| 3 | decision 5 listed the verbs in one order and their columns in another, so the sentence paired expressed with `origin`; pass 2's "no `via` at all" for `backfill_thought_actors` was false (050 sets one by `set_config`); gate 1's B and D were quoted as measured where the README marks them simulated and derived; "stands word for word" of a bullet this PR appends a sentence to; "the one thing a replay fills" beside a second thing; the snapshot trigger's exclusion described as the prototype's; "5.0 over the rows" without its n | maintainer read + run-it | each reworded from the source line |
| 3 | as the builder of SMD-2115: the backfill's source order (the first content-moving update's `before`, the tombstone's `previous_content`, the live row, else counted), the amendment's own setting value and `diff` as a fifth removed column, and a function-borne capture carrying a backdating writer's `created_at` (today only the raw path fills it) were unsaid; a row with no capture event had no rule; the lock enumeration read as every function's | maintainer read as the next builder | the step-1 row, the step-3 row and "Not decided here" say each; the locks per function |

**Boyscout.** After the stop signal, the tidy-ups the passes set aside, no behaviour changed: SMD-1998's projection contract in `evals/projection-replay.ts` credited `embed.ts` with `EMBEDDING_PROMPTS`, which `db/config.mjs` defines and `server-portable/embed.ts` applies — the string is corrected in the program and, identically, in the README's verbatim block, so the two still agree (the self-check pins no such literal); six seam lines in the record where two passes' cuts met, rewrapped to the file's width.

**Follow-ups.** Filed: SMD-2115, SMD-2116, SMD-2117 (the path; High, Medium, Medium) and SMD-2118 (a fork of the brain at a point in time as a bounded fold into a tier that writes on; Low, blocked by step 3). Not filed, with the reason on SMD-2118: multi-agent conflict preservation (the log keeps every write in `seq` order under the row lock; what the row shows is consolidation's question — SMD-1294, SMD-2011) and projector exactly-once (the check makes the fold idempotent; SMD-2117 probes it). `node_state` (SMD-2074) is named the first read-model fold to build on the log. A boyscout candidate outside this branch's files: `evals/README.md`'s gate-1 section attributes `EMBEDDING_PROMPTS` to `embed.ts` too.
