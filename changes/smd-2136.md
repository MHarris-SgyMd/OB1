---
type: added
bump: minor
tickets: [SMD-2136]
migrations: []
---

## Changelog

`bun db/ingest-records.ts --source items --items <file.jsonl>` (`-` for stdin) ingests ingestion-contract items a parser in any language emitted, one JSON object per line, through the pipeline's own write path; the new `db/ingest-items.ts` validates each line in the contract's terms and a malformed line refuses the whole file with its line number and field, exit 2, before any write — so the four import recipes the class decision routes onto the contract keep their parsers and need no database client (SMD-2136).

## FORK

The pipeline takes ingestion-contract items from a file — a parser in any language emits them as JSONL, `db/ingest-items.ts` validates each line in the contract's terms and refuses a malformed file whole, and the import recipes keep their parsers with no database client (SMD-2136)

**What changed.** `db/ingest-items.ts` is the contract's third adapter, and
not a map: the file adapter. A line is one JSON object with exactly the keys
`Ingested` names for an item — `identity {system, key}`, `scope`,
`canonical {form, mediaType}`, `text`, `links`, `mentions`, `facets`, and
optionally `createdAt` and `watermark {key, value, asOf?}` — and `parseItems`
turns a JSONL text into items, throwing `ItemsRefusal {label, line, field,
reason}` at the first line that is not one. The rules are the contract's own
(`SYSTEM_RE`, `IDENTITY_MAX`, `LINK_RELATIONS`, `ENTITY_TYPES`,
`normaliseLinks` / `normaliseMentions`) plus what the columns cannot hold: a
NUL or a lone surrogate anywhere in the line (a `text` or `jsonb` cast would
abort the run on line N of M with N−1 written), a `createdAt` or `asOf` that
is not an instant a `timestamptz` cast accepts — `Date.parse` takes February
the 30th as March the 2nd, so the calendar date is round-tripped as the Linear
adapter's `isCalendarDate` does; PostgreSQL has no year 0 and no offset past
`+15:59`, so neither passes, and a fraction past six digits is rounded at the
cast, once across a calendar day, so it stops at six — a byte that is not UTF-8 (the file is read as
bytes, each line decoded strictly, never repaired to U+FFFD; a UTF-16 file is
named as such, with or without its mark), an object or array at level 64 or
deeper, the line's object level 0 (PostgreSQL's jsonb reader stops at its
stack, at the cast; a scalar there is no container), a `system` that is one of the pipeline's own six sources (a file's row
on the board sync's id for a ticket would overwrite the sync's row with no
`held` to say so), a watermark under a key the pipeline overwrites (`source`,
the actor keys), and what the pipeline's knobs could not act on: a scope with
a `/` (`--allow` reads one as a path) or a `,` (its separator) or surrounding
whitespace, a key with surrounding whitespace (a link's target is trimmed). `null` for `createdAt`
or `watermark` is absent, what a Python emitter writes for `None`. Two lines
of one identity are refused together: they would land on one row, the second
silently over the first. `derived` is not taken from a file — a part that is
a thought of its own is a line of its own. Blank lines are skipped and still
counted, so the line a refusal names is the line an editor shows; a CRLF file
and a byte-order mark are read as the file they are.

`db/ingest-records.ts` gains the source `items`: `--items <file.jsonl>`
(`-` reads stdin), `--source items` to take the file alone. Each item becomes
a `Doc` through `docOf` on `recordId(system, key)` — the row labelled with the
item's OWN system (`metadata.source`), not `items` — and `Doc.source` widens
from the five names to any system. The write path is untouched: the row with
003's fingerprint under the ingester's envelope, the metadata merged, the
watermark judged, the canonical in `thought_sources` byte for byte (the round
trip holds by construction — the canonical IS the line's `form`), 053's links
as a set, the mentions under `source:<system>`, the vector left for
`reembed.ts`. The items carry a structure, so `applyAllowlist` gates them as
it gates the two adapter sources: `--allow <scope>` / `OB1_INGEST_ALLOW`,
default nothing, the refusal counted under `items` and said once per scope;
an item whose text another record holds is dropped as every source's is, and
named on stderr with the holder. A malformed file is a wrong input, as a
missing file or a directory is: exit 2 with the flag, the line and the field,
the file refused whole — the gather precedes every write, so no row of a
refused file is ever half in. `db/README.md`'s sources
table has the row and a section with the six-line Python emitter
(`json.dumps(item)` per line) an import recipe copies.

**Why.** SMD-2126 decided the class: an import recipe becomes an adapter of
the ingestion contract, and three of the four (`chatgpt-conversation-import`,
`perplexity-conversation-import`, `readwise-import`) are Python with parsers
worth keeping — a ChatGPT export's tree walk, an `.xlsx` reader, the Readwise
API's paging; the fourth is a standalone `.mjs`. The contract had two
adapters, both TypeScript inside `db/`, and the pipeline read a fixed list of
sources: without a seam each port would have been a TypeScript rewrite under
`db/` or a database client of its own, the shape SMD-2126 is retiring.

**Held.** `bun db/ingest-items.ts --self-check` (CI's "Ingestion adapters"
step): a good line carried field for field, the optional keys absent staying
absent, `normaliseLinks` counting the self link, the duplicate and the empty
target; sixty-four malformed kinds, each refused on the line given naming
its field in the flag's own words, after a first line of another identity so
a rule mutated away fails on its own field; blank lines skipped without
shifting numbers; CRLF and a byte-order mark as text and as bytes; a bytes
file with no trailing newline keeps its last line, good or bad; a byte that
is not UTF-8, a raw NUL byte and a UTF-16 file refused with their line; two
lines of one identity refused together, the same key under another system
not, `(ab, c)` not `(a, bc)`; `null` as absent; an array at level 63 taken
and at 64 refused; a name of exactly 200 and a key of exactly 512 taken, and
300 emoji counted as 300; UTF-16 named with and without its mark in either
byte order; the instant rule over the shapes it takes, the fourteen it
refuses and the two bounds' last accepted values. `bun db/ingest-records.ts --self-check`: a file's items as Docs
on their systems' id spaces, the structure, scope, createdAt and watermark
carried, the allowlist gating them by scope, a malformed third line refusing
the file naming the flag. `db/test-live.ts` [19] drives the CLI end to end
against the suite's server (the pipeline's write path is Bun SQL, which
`test-schema.ts`'s PGlite cannot run): `--dry-run` counts by system and
writes nothing; two items write two bare rows labelled with their system,
dated by their `createdAt`, under the ingester's envelope, two canonicals
byte for byte, one active link by identity, one mention under
`source:<system>`; the same file again writes nothing; a file whose third
line is bad exits 2 naming line 3 and the field with nothing on stdout and
zero rows for the VALID second line as for the bad one — a writer that wrote
each line as it parsed would have written it; an item whose scope is not
cleared is counted as refused under `items` with the knob named, and not
written. The reserved list is held equal to the CLI's source list by the
pipeline's self-check.

**Review passes.**

| Pass | Finding | Caught | Fix |
| --- | --- | --- | --- |
| 3 | the depth message, label, README and record said "a value at level 64" where `tooDeep` bounds containers and a scalar at 64 passes, and "or below" read two ways; a 7–9 digit fraction passed and the cast rounds it — `23:59:59.9999999` onto the next day, the rounding the docstring objects to in `Date.parse`; offset minute 60 and a lone LOW surrogate had no fixture (two mutants survived) and four more survived (big-endian mark, two of the three pipeline keys, an empty watermark value, a long subtype); a link target had no bound where the key has one and 053 drops it into a count; lengths counted UTF-16 code units, so 300 emoji were "600 characters"; an unreadable file and `--items ""` exited 1 with a trace and 0 as "skipped"; the BOM message fired on any bad line holding a U+FEFF; UTF-16 without its mark still read "holds a NUL byte"; the record said five mutants where seven survived | cold read; run-it; mutant | "an object or array at level 64 or deeper" everywhere; the fraction at six; the contract's `normaliseMentions` counts characters too (it dropped 200 emoji as 400, silently); a fixture for each survivor and a loop over the three pipeline keys; the target bounded; `[...s].length`; exit 2 by name for both; the mark blamed only when dropping it makes the line parse; every-other-byte-zero named as UTF-16; the count |
| 2 | `isInstant` took year `0000` and an offset past `+15:59`, both "out of range" at the `timestamptz` cast — the mid-run abort the module says it prevents, in pass 1's own rewrite; a scope with a `,` could never be cleared (`--allow`'s separator), the slash's class; seven mutants survived, five at exact bounds (the bytes file's last line without a newline, a NUL inside an array, the depth bound's edge, the name and key bounds, second 60 and a ten-digit fraction), and the depth fixtures matched the record's "63 taken, 64 refused" under neither counting; a FIFO as `--items` was refused with everything but a plain file; the emitter passed a dict as `canonical.form`; the RangeError catch is unreachable in Bun (its JSON.parse is iterative); a UTF-16 file read "holds a NUL byte" and a BOM inside a line was refused with an invisible character | cold read; run-it; mutant | year ≥ 1 and offset hour ≤ 15 (`MAX_TZDISP_HOUR`), with the bounds' last accepted values probed; the comma refused; a fixture at each bound and the level counted from the line's object as 0 in the message, README and record; a directory alone refused, by name; `json.dumps(conv.raw)`; the catch's comment says what it is for; both files named for what they are |
| 1 | the bad-file leg could not tell "refused whole" from a line-by-line writer — its valid lines were already rows; `createdAt: null` refused where a Python emitter writes `None`; `isInstant`'s docstring claimed the cast's whole grammar and `Date.UTC` read a year under 100 as 19xx; a scope with `/` was resolved as a path by `--allow` and could never be cleared, and a padded scope or key could never be cleared or linked; a byte that is not UTF-8 was repaired to U+FFFD, the canonical no longer the source's; a facet nested two hundred thousand deep died with a stack trace, exit 1; a file could claim `linear`, `markdown` or `items` and land on the sync's id with no `held`; a directory as `--items` died with EISDIR; a BOM was refused with an invisible character; the watermark message promised an instant the code did not ask for; a dropped duplicate text was a count, not a name; `--items` beside another `--source` was ignored silently; three mutants survived (CRLF, a whitespace scope, the identity map's separator); the malformed fixtures failed as duplicates of line 1 when a rule was mutated | cold read; run-it; mutant | a new valid line in the bad file, asserted absent; `null` is absent; the docstring and `setUTCFullYear`; the six reserved systems held equal to `SOURCES`; the slash, whitespace, UTF-8 (bytes in, each line decoded strictly), depth-64 and directory refusals; the BOM dropped; the message; each dropped item named with its holder; the note; fixtures for each, after a first line of another identity |

**Not taken.** The REST gateway as the Python recipes' target — decided
against in SMD-2126 (`open-brain-rest`'s `/capture` embeds at 1536, which the
fork's default brain refuses). A TypeScript adapter per recipe under `db/`:
the parsers are the recipes' value and Python. `derived` from a file: a
recursive shape for a case no import needs — each extracted thought is its
own line. Skipping a malformed line and writing the rest: a file half written
cannot be re-run cleanly, where a file refused whole is fixed and run again.
A file-level `scope` or `system` header: each line is complete, so a file can
be concatenated from several runs and a line read on its own.

**Follow-ups.** The four import ports SMD-2147–2150 and `pull-gmail.ts`
(SMD-2021) emit items for this flag and each removes its
`POSTGREST_EXCEPTIONS` entry when it lands.
