---
type: fixed
bump: patch
tickets: [SMD-2106]
migrations: []
---

## Changelog

The MCP route no longer answers a refused JSON-RPC **notification** with an HTTP 200 error envelope — a shape the MCP TS SDK silently drops, so `notifications/initialized` and `notifications/cancelled` looked delivered when they were refused. A notification-only body now gets no JSON-RPC body: `202` for a refusal that cannot change on retry (no, wrong or revoked key), `503` with `Retry-After` for one that can (the agent registry busy). A request still gets its 200 envelope, and the busy request now carries `Retry-After` too (SMD-2106).

## FORK

The MCP route answers a refused notification per spec: 202/503 with no body, not a dropped 200 envelope (SMD-2106)

**What changed.** `server-portable/index.ts`: the two pre-transport auth
refusals (no/wrong key at the `!principal` site; revoked/busy at the
`resolve()` site) now branch on whether the body expects a reply. A new
`refusalTarget(bodyText)` returns `expectsReply` false only when the body is
positively notification-only — every message a plain object with a string
`method` and no `id` member (the `id`'s presence, not its value, marks a
request: `id: null` is still a request). A request, a response, a malformed
body, a mixed or empty batch all keep the 200 envelope, so a reply is suppressed
only where JSON-RPC forbids one. `notificationRefusedResponse({ retryAfter? })`
returns a bodyless `202` (no retry) or `503` + `Retry-After` (busy);
`unauthorizedResponse` gains a `retryAfter` option so the busy REQUEST envelope
carries the header too. `RETRY_AFTER_SECONDS = 2`. `corsHeaders` gains
`Access-Control-Expose-Headers: Retry-After` so a browser-hosted client — the
strict host the 200-envelope design exists for — can read the delay off a
`fetch` (it is not a CORS-safelisted header; review pass 2).

**Why.** JSON-RPC 2.0 says the server MUST NOT reply to a notification; MCP
streamable HTTP says a notification the server accepts is `202` with no body, one
it cannot accept an HTTP error. The route answered `200` with a
`{error, id: null}` envelope for every refusal, inherited from the `-32001`
path. The MCP TS SDK (1.30, `client/streamableHttp.js`) cancels the body of a
`200` that held no request, so during a registry lock `notifications/initialized`
and `notifications/cancelled` were dropped and the client believed them
delivered (SMD-2072 review pass 2 measured a 252 ms `200` carrying
`{"error":{"code":-32003},"id":null}`). Harmless on this sessionless server
today, but wrong on the wire.

**Held.** `bun server-portable/test-server.ts`, 297 assertions (from 285): a
refused notification (missing key, wrong key), an all-notifications batch, a
mixed batch and an `id: null` request are each answered correctly — 202 no body
for the notifications, the 200 envelope for anything holding a request; a
non-string `method`, an empty batch `[]`, a JSON `null` body and a `[null]`
element each keep the envelope (not a 202 and not a throw), and every answer
exposes `Retry-After`. `bun server-portable/test-e2e-sql.ts`
against real Postgres, 229 assertions (from 226): during a registry lock a revoked
key's notification is `202` no body, a busy key's is `503` no body with
`Retry-After` (exposed for a browser), and the busy request keeps its 200
envelope now carrying `Retry-After`. Server typecheck clean.

## Review passes

| Pass | Finding | Caught by | Fix |
| --- | --- | --- | --- |
| 4 | No correctness defect — the convergence pass. The cold read re-read the full branch diff with fresh eyes and cleared the angles the prior passes had not: only per-request state on the refusal path (no shared mutable state, so concurrent requests cannot interfere), the body consumed at most once, only two refusal exits, `Response(null)` correct, Retry-After-on-a-200 harmless, and the merge base clean. The run-it pass killed 7 more boundary mutants (202→204, `every`→`some`, the empty-batch length guard, the id echo, Retry-After dropped on each of the three paths) and found only 2 provably EQUIVALENT survivors (the empty-string parse guard and the `!Array.isArray` defence-in-depth) — no gap | cold-read + run-it + mutant | none needed — the change held; the two survivors are equivalent mutants, recorded here |
| 3 | No correctness defect — the confirming pass. The cold read verified pass 2's `Access-Control-Expose-Headers` fix is correct, needed (Expose-Headers governs readability even under `Allow-Origin: *`), non-duplicating across every response path, and correctly index.ts-local, and that the two teeth are non-vacuous. The run-it pass killed every core mutant with no survivor except one: the `m !== null` guard in `isNotification` was load-bearing but unpinned — a JSON `null` body or a `[null]` element would throw `(null).method` without it (a 500 where the design promises the 200 envelope), and no assertion held that | cold-read + run-it + mutant | teeth pin a `null` body and a `[null]` element at the 200 envelope (verified: removing the guard fails them 295/297); no code change |
| 2 | No correctness defect on the real path — both reviewers ship-ready. One real gap and two coverage gaps. The cold read found `Retry-After` was not in `Access-Control-Expose-Headers`, so a browser-hosted MCP client (claude.ai, the Claude Desktop connector — the strict host the whole 200-envelope design targets) could not read the busy delay off a `fetch`; the status/`-32003` code still reach it, only the machine-readable delay was lost. The run-it pass found two survivors, both correct-but-unpinned: a non-string `method` body and an empty batch `[]` each keep the 200 envelope, and no assertion held that | cold-read + run-it + mutant | `corsHeaders` exposes `Retry-After`; teeth pin the non-string-`method` and empty-`[]` bodies at the 200 envelope, and the e2e busy notification now asserts the header is exposed |
| 1 | No correctness or safety defect. The cold read traced every classification edge — `!("id" in m)` keys on presence not value (so an `id: null` request keeps the 200 envelope, a genuine notification gets 202/503), and anything not positively all-notifications (null/number/string/`[]`/mixed/malformed bodies) keeps the envelope, so a reply is suppressed only where the spec forbids one; `Response(null)` is a genuinely empty body, Retry-After is wired at all four exits, no double body-read, no leak. The run-it pass killed all 7 mutants with no survivors and confirmed non-vacuity (a sentinel swap on both suites fails). One docblock wording nit | cold-read + run-it + mutant | the `unauthorizedResponse` docblock is reworded to say it is the request shape and its bodyless twin sets Retry-After too; no code change |

## Not taken

A distinct HTTP status for a malformed or mixed-batch refusal: the route keeps
the 200 envelope for anything not positively a notification, so a request is
never answered with a bodyless status by mistake — the conservative choice, and
the transport still 400s a malformed body it is handed. `Retry-After` as an
HTTP-date rather than delta-seconds: the value is advisory and "a few seconds"
is what BUSY_MESSAGE promises.

## Upstream status

Not upstream.
