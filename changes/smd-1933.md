---
type: added
bump: patch
tickets: [SMD-1933]
migrations: []
---

## Changelog

The connector taxonomy: every external-touching artifact is classified by family × transport × direction × cardinality × round-trip per capability in `docs/connector-registry.json`, one connector per vendor with its direction derived, a family schema per family as the fetcher-agnostic seam, and check 18 holding the registry sound, the spec's tables current and the coverage complete (SMD-1933).

## FORK

The connector taxonomy — five facets per capability, one connector per vendor with its direction derived, one family schema per family as the seam any fetcher lands on, held by check 18 (SMD-1933)

**What changed.** `docs/connector-taxonomy.md` is the spec and
`docs/connector-registry.json` the one source behind it: the four closed and
near-closed facet sets (`direction`, `round_trip`, `transport`, `cardinality`),
the open `family` set with a schema per family (item, grouping key, canonical,
text, edges, metadata, identity, dividing line — SMD-1867's five outputs named
from the input side), the four fetcher kinds (low-code node, native driver, MCP
server, browser extension), and every external-touching artifact with its
capabilities. Twenty-two artifacts, thirty-two capability rows, sixteen
connectors. A **connector is a vendor**: the registry declares its direction
once and the check derives it from the capabilities that name the vendor, so
`telegram` (two sources, three sinks, five artifacts), `gmail`, `discord` and
`slack` are one `bidirectional` connector each instead of the separate capture
and digest artifacts they were. `scripts/connector-registry.mjs` renders the
family schemas and the two tables into the spec between markers; the prose
around them is hand-written. `CONTRIBUTING.md` says when a contribution is a
connector; `docs/vendored-disposition.md` points at the classification.

**Why.** SMD-1924's triage found the same vendor implemented several times —
`gmail` a source twice and a sink once, `telegram` a source twice and a sink
three times — each artifact carrying its own client, and no seam that would let
the orchestration tool SMD-1863 picks land a vendor's existing node in the
brain without an OB1 connector. The ticket's own list of "external-touching
artifacts" was prose; nothing could say whether a new recipe touching Notion
was classified. Two families the ticket's seven did not cover surfaced at
classification time: `calendar/event` (life-engine reads Google Calendar
through the client's MCP connector) is declared; `web-clip` has no instance
(the Chrome extension captures conversations) and stays declared for the
clipper that will.

**Held.** Check 18: `registryProblems`, one pure function in
`scripts/connector-registry.mjs`, run over an in-memory baseline first and then
one mutant per rule kind, each expected to yield exactly its kinds — a
transport off the set, a fourth transport added to the registry's own set, a
sixth facet, a fifth fetcher, an undeclared family, the reserved family used, a
schema field missing, a directory that does not exist, an artifact listed
twice, a path off the shape, a capability repeated, a connector declaring
`source` while its capabilities span both, a vendor with no connector, an
external-touching artifact left unclassified (by service, by tag, by the
vendor's name as a tag, by the disposition table alone), one both classified
and excused, one classified that nothing marks, a stale or reasonless excuse,
a stale, empty, reasonless or uncompilable pattern, a registry or an artifacts
block of the wrong shape — and non-probes: a model-provider service triggers
nothing, two patterns covering one service are both live, a metadata whose
tags are a string throws nothing, a provider qualified after itself is
covered, an excuse for a directory without metadata waits. Then the real
tree: every registered artifact's `metadata.json` declares `connectors` equal
to the vendors its capabilities name (a schema field added for it), and the
coverage rule sweeps every `metadata.json`'s `requires.services`
against the not-a-connector patterns (model providers, hosting, the brain's
own surface, agent runtimes — a pattern covers a string only when its match
begins the first or the second word), the connector-shaped tags, the declared
connector names as tags, and the fold-in SMD-1867 rows of the disposition
table; the spec's generated block must equal the render, compared only when
the registry is sound.
`bun scripts/connector-registry.mjs --check` runs the same rules by hand.

**Review passes.**

| Pass | Finding | Caught by | Fix |
|---|---|---|---|
| 1 | check 18 rendered the tables after reporting the registry unsound, so a missing family field crashed the whole run and lost every other check's findings | cold read | render compared only on a sound registry; a throw is a finding |
| 1 | an empty or missing service pattern compiled to the match-everything regex and excused the whole tree in silence | cold read | refused as `pattern-invalid`, probed |
| 1 | the stale-pattern rule credited only the first pattern a service matched, so a narrower later pattern read as dead | cold read | every match counted; an overlap non-probe |
| 1 | a vendor named only as a tag (`atomizer` tagged gmail, `schema-aware-routing` tagged slack) marked nothing, and a service string a provider pattern also matches could hide a vendor | cold read | a tag equal to a declared connector name is a trigger; five excuses added; the remedy names the tag |
| 1 | five rule kinds had no mutant; string-typed `tags` or an object `artifacts` threw instead of reporting | cold read | probes for each; list guards; a `shape` finding |
| 1 | the prose carried counts the check did not hold; two contribution walkers with different skip rules | cold read | counts left to the generated line; one walker with check-fork's rules |
| 1 | `discord-capture` was described as sitting on Discord's gateway; the folder ships no code | cold read | the note says what the tree shows |
| 2 | a provider pattern excused a whole service string it matched anywhere, so "Notion API (summaries via OpenRouter)" swept as a provider | cold read | a pattern covers a string only within its first two words; one system per entry, its name first |
| 2 | the CLI rendered before reading its own findings and crashed where check 18 no longer did; an unparseable registry escaped unhandled | cold read | render only on a sound registry; parse errors reported |
| 2 | the disposition reader kept the last category past any later heading and took a bare ticket mention as a fold-in | cold read | any heading ends the context; the fold-in marker is required; probed |
| 2 | an excuse for a directory with no `metadata.json` read as "no such contribution" | cold read | existence is the directory's; the excuse waits on check 1 |
| 2 | two walkers of the contribution directories; check 18 parsed every metadata a second time | cold read | `scripts/contributions.mjs` is the one walk; check 18 reads what check 1 parsed |
| 2 | the telegram note and the record said four artifacts where the data says five; the disposition pointer promised sinks the table does not hold | cold read | five; the pointer says what the sweep reads from the table |
| 3 | the two-word window still covered "Notion (OpenRouter)" and "Gmail/OpenAI" — the provider as the second token | cold read | the match must begin the first or the second word; probed |
| 3 | the fold-in marker matched "not an SMD-1867 adapter"; a fold-in row for a removed directory vanished silently | cold read | the table's positive phrases only, a negation probe; a gone row is `disposition-stale` |
| 3 | a family declared `null` threw; the CLI still crashed outside the parse; tags compared case-sensitively against case-insensitive patterns | cold read | `?.`; every CLI step reports in words; tags lower-cased |
| 3 | the stale-pattern rule counted only head matches while the hint used any match; check 18 restated `readMetadata`'s contract inline | cold read | liveness is any match, coverage is the head; check 18 calls `readMetadata` |
| 4 | coverage inferred "touches vendor X" from prose only, and each pass found a new hole (maker-first strings, tag vs key); the "Not taken" reason was an upstream-parity argument | cold read | `metadata.json` gains `connectors`, held equal to the registry on 22 artifacts; the prose sweep stays as the net for the undeclared |
| 4 | the fold-in marker read the whole row and `docs/drafts/`; `in` on plain objects; the walk outside the CLI's guard | cold read | the Disposition cell only (11 rows moved), contribution categories only; `Object.hasOwn`; wrapped |
| 4 | `capture`/`export`/`sync` tags forced nine excuses for the brain's own vocabulary; an unparseable metadata got a coverage verdict | cold read | the trigger tags are the connector-shaped six; `null` metadata gets no verdict |

**Not taken.** The classification itself inside each artifact's
`metadata.json` — a connector spans artifacts, so the capabilities and the
family schemas live beside them; what an artifact carries is the one-line
declaration `connectors: [<vendor>]` the schema gains (review pass 4 — the
first draft declined even that on the ground that the schema is shared with
upstream's gate, an upstream-parity argument the fork has dropped, and the
schema already carries three fork fields). Dropping `notification-target` — kept reserved, unusable until
`reserved` is removed, which the check makes a visible spec change. Building
the capture node, the envelope route or any mapper — SMD-1931 owns the route,
SMD-1867 the projection, SMD-1863 the fetcher count; this change fixes the
payload and the classification they share. A hand-written classification table
— the doc's tables are rendered, so the prose cannot drift from the data.

**Follow-ups.** The two verify items that need a pipeline — a new vendor with
only a mapping, the same vendor through a low-code node and a native driver
yielding one canonical — are specified here and proven under SMD-1867 and
SMD-1863; SMD-1814's writable Obsidian moves that connector to `bidirectional`
in the registry when it lands.

**Upstream status:** not sent — the taxonomy classifies this fork's vendored
tree and holds it in this fork's check; upstream has neither.
