---
type: added
bump: minor
tickets: [SMD-2210]
migrations: []
---

## Changelog

An opt-in `orchestration` compose profile runs n8n beside the brain: pinned by digest, a SQLite store of its own (measured against a Postgres 17), loopback only, a 24-hour run-history window, its owner set from the environment; `deploy/orchestration/provision.ts --init` writes the profile's secrets and a provisioning run keeps an expiring API key in `deploy/.env` that later runs revoke when replaced, loads credentials and templates, and refuses a brain key at write scope or the wrong scope; the eval kit now runs its checks against the profile as it ships, with key, pruning and egress-probe checks (SMD-2210).

## FORK

n8n as an opt-in `orchestration` profile — pinned, its own store, expiring and separate keys, a short run-history window, and the eval kit re-run against what ships (SMD-2210)

**What changed.** `deploy/compose.yaml` gains an `n8n` service under
`profiles: ["orchestration"]`, beside `board-sync` and `jev`:
- `n8nio/n8n:2.40.6` by its multi-arch index digest, on n8n's default SQLite
  in the `n8n-data` volume.
- One port, `${N8N_BIND:-127.0.0.1}:${N8N_PORT:-5678}`, in check 13's form;
  PUBLISHES lists it.
- The telemetry switches, `N8N_BLOCK_ENV_ACCESS_IN_NODE`, and
  `N8N_DISABLED_MODULES=mcp-registry`: the egress probe caught that module
  calling api.n8n.io.
- A window of 24 hours or 1,000 runs (n8n's defaults: 336, 10,000).
- The owner set from the environment at every start
  (`N8N_INSTANCE_OWNER_MANAGED_BY_ENV`, a bcrypt hash), so a fresh instance
  cannot be claimed by whoever reaches the port first.
- An entrypoint that refuses to start without the encryption key or the
  hash. Compose's `:?` would fail every `up` without the profile.

`deploy/orchestration/provision.ts` is the provisioning step, run from a
checkout; the kit imports it.
- **`--init`** writes the missing secrets into `deploy/.env`: the key, the
  owner's password and hash (single-quoted: compose mangles a bare `$`,
  measured), and the two inbound keys, `N8N_MCP_KEY` (header `x-n8n-key`)
  and `N8N_WEBHOOK_KEY` (`x-n8n-run-key`).
- **A run** signs in through `/rest/login`. It keeps an eight-scope API key
  with its id, scopes and the file's tag (`N8N_API_KEY`, `_ID`, `_SCOPES`,
  `_TAG`), all in one write, expiring after `N8N_API_KEY_DAYS` (90). It
  mints a new key when the stored one is refused, its scopes differ from
  those asked for, its id is not listed, it has under a week left (half its
  life, if shorter), or on `--rotate`, writing it before any deletion.
- **Every run deletes every other key its env file minted**: the tag, which
  counts only beside a machine-and-path fingerprint. So one a failed run left
  goes on the next, and another checkout's file, or a copy, keeps its own.
- **It checks every template before writing**: a brain key is refused at
  write scope (the legacy `MCP_ACCESS_KEY` included), when unlisted, or at a
  scope other than its credential's declared `brainScope`, wherever in the
  credential it sits (`Bearer <key>` and `?key=` included). A template
  naming an undeclared credential is refused, and so is an unquoted hash line
  or a password over bcrypt's 72 bytes. Then credentials are created or
  patched, and templates created or replaced, through `/api/v1`.
- **`--self-check`** runs these rules against a fake n8n in CI.

The kit's n8n (`evals/orchestration/`) is now the profile. The driver passes
`--profile orchestration` and runs the profile's `--init`, and
`compose.n8n.yaml` changes only the pruning cadence. `--with postgres` gives
n8n a Postgres 17 of its own. `--with sealed` is the egress probe: an
`internal: true` network, a tcpdump watcher owning n8n's network namespace,
a Bun HTTP front, and a probe workflow of ten fixed captures. The two do not
combine. Beyond C1–C3 and C1s, `--verify` adds three checks:
- **K**: each inbound key refused on the other's path; after `--rotate` the
  replaced key answers 401, the new one's expiry is set, and one key is held.
- **P**: two saved runs moved 25 h and 23 h back in the store; the first gone
  from the API and the store, the second kept.
- **E**, fail-closed against facts read live (n8n's resolv.conf, the
  brain's addresses): every outbound packet is a readable DNS question to a
  listed resolver or a connection to the brain's :8000; every name is a
  service's or a template's host; an unreadable line, or unread facts, fail.
  The judge is pure, and `eval-orchestration.ts --self-check` runs 31 cases
  in CI.

`--up` stamps the brain's build and refuses a brain that does not report the
stamp, and the kit's n8n listens on 5680, beside an operator's 5678.

Decided here: the store is SQLite. The import pipeline's runner is a keyed
HTTP runner in the profile, a bounded write path amending decision 4, built
with SMD-2212's first import template (`docs/orchestration-tool.md`,
"Running a pipeline from a workflow").

**Why.** SMD-1863's ADR left the store to measurement and the profile to this
ticket. The POC ran n8n on the brain's Postgres 16, which n8n calls
"compatibility support only". Its key did not expire and stayed valid after
a re-mint, one key served two paths, and nothing probed what n8n dials.

**Held.** CI's "Every published port binds loopback" step pins three things:
n8n's port to loopback, its image to a digest, and a plain `config` to
`migrate postgres server`. `provision.ts --self-check` holds the rules
above, and `eval-orchestration.ts --self-check` holds E's judge. The kit
(`--verify n8n`) holds the rest against a live profile.

**Measured after.** On the dogfood Mac (podman, the host's Ollama), one cycle each.
- **SQLite and `--with postgres`** (implementation commit, `--wait-schedule`):
  C1–C3, C1s, K and P pass. Captures took 20.4 s and 22.8 s; the schedule was
  seen after 811 s and 752 s. SQLite again on pass 3's code: the same, 811 s.
- **Review passes 3 and 4's code:** the plain profile passed C1–C3, K and P, and
  sealed passed C1–C3 (the act tool refused), K, P and the fail-closed E.
- **Egress:** the sealed n8n asked only for `api.linear.app`. Every packet it
  sent out was a question to the network's resolver or one of seven
  connections to the brain, and no line was unreadable, over about 2¼ min.

The store is SQLite:

| store | n8n | its own server | extra image | on disk |
| --- | --- | --- | --- | --- |
| SQLite | 369 MiB | none | none | 6.4 MiB |
| Postgres 17 | 404 MiB | 52 MiB | 280 MiB | 15.5 MiB |

`deploy/README.md`'s backup (`VACUUM INTO`) and restore were run as written,
over a stopped n8n and into a fresh volume. The copy came back (a marker
workflow added after it was absent), integrity was ok, and the key answered.

**Review passes.**

| pass | finding | caught by | fix |
| --- | --- | --- | --- |
| 1 | the documented restore over a stopped n8n let SQLite replay the old WAL onto the copy: "database disk image is malformed" | run-it | the restore removes the WAL and SHM first; both shapes walked from the doc |
| 1 | a fresh n8n let whoever reached the port first claim the owner | cold read | owner set from the environment; `--init` |
| 1 | a key a failed run left undeleted stayed valid while later runs reused theirs | cold read | every run sweeps; the key's id kept |
| 1 | E judged DNS names only: a raw-IP dial, and a sealed network made non-internal, passed | run-it | E judges dials and UDP too; self-checked |
| 1 | the brain-key check read one header type, missed the legacy write key, and let a read key sit in the capture credential | cold read | any field, `brainScope` declared per credential |
| 1 | the runner section called the runner's write path "decision 4 holds" | cold read | named as an amendment to decision 4 |
| 1 | the template-id loop read nothing and a new env file's mode was untested (two mutants survived) | mutant | checked at run time; fake-n8n self-check |
| 1 | the operator's key carried the kit's run-history reads; a 429 read as a wrong password; a short key re-minted every run | cold read | eight scopes, the kit asks for its two; 429 named; renewal at half-life |
| 1 | `--with postgres --with sealed` was accepted and cannot work | cold read | refused, with why |
| 1 | numbers: a 2⅓-minute window called four, "a third" of the disk (41%), "~90" scopes (106) | run-it | record corrected |
| 2 | E: a name starting like a service (`n8n.io`, `server.…nip.io`) counted as internal and made its answer an allowed address; EDNS/NS queries, decoded UDP (NTP) and port 5678 anywhere went unjudged | cold read, run-it | E fail-closed: outbound only, facts from resolv.conf and the engine, any query type, every other line a dial |
| 2 | two env files on one n8n revoked each other's key every run | run-it | keys tagged per file |
| 2 | key, id and scopes were three writes: a run killed between them left a key the next sweep deleted | run-it | one write; a listed id required; the key probed after the sweep |
| 2 | a brain key inside `Bearer <key>` or `?key=` passed; an unquoted hash line passed `--init` and reached n8n mangled; a >72-byte password could never sign in | cold read | tokens hashed; quoting enforced; 72 bytes refused |
| 2 | nothing in CI read compose's owner block (a live mutant removing it let a stranger claim the owner); the fake ignored cookie, email and scopes | run-it | self-check parses compose; a faithful fake |
| 2 | the kit's n8n took the operator's port 5678 | cold read | 5680 |
| 3 | a copy of an env file carried its tag, so the two revoked each other's key after either minted | cold read, run-it | the tag counts only beside a host-and-path fingerprint |
| 3 | five DNS query shapes (CD flag, notify, unknown type, CHAOS, TCP) hid their names, and the resolver forwarded them when the seal leaked | run-it | data to the resolver must carry a readable question; TCP 53 recorded |
| 3 | IPv6 SYNs were never captured; a loopback resolver (Docker) left E judging no names | cold read | IPv6 TCP recorded; a loopback resolver cannot be judged, and fails |
| 3 | the self-check lacked cases five mutants survived; the revoke-before-write order was untested | mutant | 31 cases; a read-only-directory leg |
| 4 | the fingerprint hashed the hostname, which follows the network on this Mac and is random in a container, so a file orphaned its own keys | cold read, run-it | the machine's stable id and the real path; `--adopt` for a moved file's keys |
| 4 | a symlinked env file was replaced by a regular file, and the secrets missed their store | run-it | writes go through the link |
| 4 | E's PASS did not show the capture ran for the whole window | cold read, run-it | one header, no exit summary, the watcher running and started before n8n |
| 4 | the documented bump never ran E; "restart" kept the old hash; a restore brought revoked keys back; "gone" meant rows, not bytes | cold read | a two-cycle bump after a backup; recreate; restore ends `--rotate`; said |

**Not taken.** A one-shot provisioning service with no standing key (the
maintainer's choice). An n8n image with Bun and python3 for the import
pipeline (OB1 would distribute n8n). A compose one-off the workflow starts.

**Follow-ups.** SMD-2212 builds the runner with its first import template.

**Upstream status.** Fork-only.
