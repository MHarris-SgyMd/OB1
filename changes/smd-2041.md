---
type: added
bump: minor
tickets: [SMD-2041]
migrations: []
---

## Changelog

`brain_info`, a read-scoped MCP tool, and `GET /health` with a read or write key answer what a brain is — version and release range, the commit the image was built from, store, tier, Postgres and pgvector versions, schema version, the ledger's highest migration against the server's own, row counts, size, HNSW parameters; preflight's version, ledger and pgvector rows read the same facts, and `initialize` reports the fork's version instead of `1.0.0` (SMD-2041).

## FORK

A brain says what it is at runtime — `brain_info`, a keyed `/health` body, and preflight reading the same facts (SMD-2041)

**What changed.** `server-portable/brain-info.ts` holds one read.
`readDatabaseFacts(sql)` asks the database, each fact its own statement so a
refused read (named in `unread`) leaves the rest: `server_version`, pgvector's
`extversion` and schema, `ob1_config`'s `schema_version`, `embedding_model` and
`embedding_dim`, the ledger (visible, readable, every name, the highest prefix),
the rows in `thoughts`, `thought_audit`, `thought_chunks` and `ob1_entities`,
`pg_database_size(current_database())`, and every HNSW index on the search path
with its `m` and `ef_construction` (pgvector's defaults when built with none).
`brainInfo()` sets the server's own facts beside it — the version, the release
range `releases.json` records for it, the tree's last migration, the commit
(`OB1_GIT_SHA`, `unknown` when unset), the store, `OB1_TIER`, the configured
embedding model and width — and judges the ledger `current`, `behind` or `ahead`
of the tree; it never raises (a database that cannot answer is `database.error`).
`ThoughtStore.databaseFacts()` is the SQL store's pool running the read; the
PostgREST store refuses, since PostgREST exposes no catalog reads.

Two surfaces, one function. The `brain_info` tool (`tools.ts`, read scope,
`readOnlyHint`) renders a short table. `GET /health` with a read or write key
answers the record as JSON; no key, a wrong key, a capture-only key and a key
revoked in the registry all still get the literal `ok`, so nothing about the
deployment reaches an unauthenticated probe, and a keyless probe never dials the
database. The status is 200 either way — the process is serving. The answer is
bounded. The read is one transaction on one connection: the catalog facts in
one statement, each read a role or a lock can refuse in a savepoint, the
timeouts set once as ceilings (a stricter role keeps its own) — 800 ms per
statement and 300 ms per lock wait for the health body, 5 s and 1 s for the tool
and preflight. Each unread fact carries its reason by SQLSTATE (`refused`,
`timeout`) or `deadline`, `invisible`, `error`; the facts read by the deadline
(`HEALTH_DEADLINE_MS`, 2.5 s; the tool 15 s) are kept. Concurrent probes share
one read. A registry with no answer by the deadline gets `ok`, since it could
still say revoked; a keyed `HEAD` is the bodiless `ok` and reads nothing. A
table is resolved by `to_regclass` and confirmed absent by `pg_class`: one that
exists where the role cannot reach it is `invisible`, never "no table", so
preflight never answers it with `--baseline`. Preflight's read skips the counts
and the size.

Preflight's `vector extension` row now names pgvector's version, and its
`migration ledger` and `schema version` rows read `readDatabaseFacts` in place of
their own reads (the ledger set every ledger-aware remedy consults comes from it
too), so the gate and the tool cannot report two databases. The ledger row judges
the highest migration against the tree the server was built from: behind warns
with the migrate command, ahead warns that a newer tree migrated the brain.

The version module. `db/version.mjs` is node-only and the Workers build bundles
the server, and neither a Worker nor the image holds `releases.json`'s ranges or
`db/migrations/`. `scripts/gen-version.ts` writes `server-portable/version.ts` —
`FORK_VERSION`, `RELEASE_RANGE`, `LATEST_MIGRATION`, no imports — and
check-fork-consistency's **17e** round-trips it; `assemble-release --write`
regenerates it after writing `releases.json`, and `CONTRIBUTING.md` says a new
migration reruns it. The MCP `initialize` reply read a literal `1.0.0` that
predated the version scheme and never moved through `1.1.0`; it reads
`FORK_VERSION`.

The commit. `server-portable/Dockerfile` takes `OB1_GIT_SHA` as a build arg into
the environment, after every `COPY`. Compose passes it to the server and the
three tier servers; the release job passes the tag's commit to both server
builds; the CI stack writes it into `deploy/.env`. Check 14 excuses it from the
runtime forward (`NOT_FORWARDED`): a forward would let `.env` name a commit the
image was not built from. `deploy/smoke.sh`'s check 10 reads the keyed body
without following redirects (curl forwards a custom header to the next host),
prints the version, commit and highest migration, and asserts the version equals
the checkout's `version.ts` and, with `OB1_SMOKE_COMMIT` set (CI sets it), the
commit.

**Why.** An agent or operator connected to a brain could not ask it what it was.
Preflight read the version, the ledger and pgvector once at container start;
`thought_stats` counted thoughts; `/health` said `ok`. Nothing read the commit,
Postgres's version, the database's size, the audit count, the tier or the HNSW
parameters, so "is the dogfood brain on 052 yet?" was answered by a scratch
script over the compose network after SMD-1296 landed, and a resuming agent
could not tell which server it was talking to — `initialize` said `1.0.0` to a
`1.1.0` server.

**Held.** test-server [7] (`serverInfo.version` is `db/version.mjs`'s
`FORK_VERSION`), [10b] (the tool with no database: the version row, the store's
refusal on the Database row), [13b] (keyless, wrong-key and prefixed probes get
`ok`; keyed probes get JSON whose `latestMigration` is `db/migrations/`'s last,
read from the directory). test-auth [8b]: read and write keys get the record, a
capture key (header and URL) and a missing or wrong key get `ok`. test-e2e-sql
[14] against real Postgres: no ledger reads as unjudged; after
`migrate.ts --baseline` the highest is the tree's last and `current`; versions,
counts, size and both HNSW indexes equal the catalog's; nothing unread; the
tool's rows agree; one ledger row deleted is `behind`; a revoked key gets `ok`.
test-preflight: the vector row names the catalog's pgvector, and the ledger
current, behind and ahead, the version row reading the same highest migration.
Check 17e: a hand-edited `LATEST_MIGRATION` fails naming the command.

| Mutant | Killed by |
|---|---|
| the shared read's highest migration off by one | test-preflight 3, test-e2e-sql 3 |
| pgvector's version a constant | test-preflight 1, test-e2e-sql 1 |
| `/health` admitting a capture-only key | test-auth 2 |
| revocation ignored at `/health` | test-e2e-sql 1 |
| the keyed body dropped (`ok` for every key) | test-server 15 |

**Review passes.**

| Pass | Finding | Caught by | Fix |
| --- | --- | --- | --- |
| 1 | A keyed probe at an unreachable address, or while a migration held `thought_audit`, got no reply at all (8–12 s, the runtime's idle sweep): the driver's 30 s connect and an unbounded `count(*)` | run-it | per-read statement and lock timeouts; the registry check and the read under one deadline; held by test-server [13c] (a listener that never replies) and test-e2e-sql [14] (a held lock) |
| 1 | The ledger's presence came from `information_schema.tables`, which hides a table from a role with no privilege on it — the documented server role was told its brain had no ledger and to `--baseline` it — and matched the name in any schema | cold read, run-it | `to_regclass('schema_migrations')`; preflight's remedy moves with it; held by the limited-role cases in test-e2e-sql [14] and test-preflight |
| 1 | Two reads were not guarded, against the docstring; check 17e (and the tools.json round-trip) passed under bun when its generator failed to import | cold read | guarded; a skip only off bun |
| 1 | Eleven mutants survived — the `ahead` judgement, `unread` recording, an unreadable ledger read as empty, the unread-`ob1_config` branch, HNSW options, byte units, the commit | run-it | test-server [10c] (a fake client: a refused and an absent read, the rules, the rendering, the deadline), the limited role, a `WITH (m = 24, ef_construction = 100)` index, the commit in [13c]; all re-run and killed |
| 1 | The top-level `ledger` status beside `database.ledger`, every migration name in the body, an unread `ob1_config` rendered as "none recorded", a keyed `HEAD` paying the read, smoke's tab split collapsing an empty field | cold read, run-it | `ledgerStatus`, `{ present, readable }`, `?`, HEAD reads nothing, `\x1f` |

| 2 | Pass 1's reads, a transaction each, added their budgets past the one deadline: three locked tables lost the whole database half, and forty probes against a lock held the pool while tool calls queued | run-it, cold read | one transaction, savepoints, the catalog in one statement, facts kept at the deadline, one read in flight; held by test-e2e-sql [14] (three tables locked, ten probes, one waiting backend) and test-server [10c] (a hanging count) |
| 2 | A revoked key read the whole record while the registry's tables were locked — the resolve lost the race and passed for "not revoked" | run-it, cold read | no answer by the deadline is `ok`; held by test-e2e-sql [14] with `ob1_agents` and `ob1_agent_keys` locked |
| 2 | A lock or statement timeout on the ledger read as "not readable by this role", with the grant remedy; a ledger off the role's search path (or no USAGE) read as absent, with `--baseline` | cold read, run-it | reasons by SQLSTATE, `invisible` from `pg_class`; held by test-preflight (the ledger locked; a role with public off its path) and test-e2e-sql [14] |
| 2 | Surviving mutants: the timeouts made session-wide (every later query on the pool capped), a stricter role setting raised, `clearTimeout` dropped; preflight read counts it never used; the Dockerfile's comment claimed the baked commit could not be overridden | run-it, cold read | `SHOW` after the read on its connection, a role at `lock_timeout` 100 ms, `stats: false`; the comment and check 14's reason corrected |

| 3 | The health route's registry resolve, raced but never cancelled, left one pool connection waiting per probe while the registry's tables were locked: ten probes emptied the pool and every tool call timed out while /health said `ok` | run-it | one resolve in flight per key; held by test-e2e-sql [14] (ten probes of one key, the waiting count unmoved) |
| 3 | The single-flight map was keyed by the deadline value (a shared entry gave a probe the tool's 15 s deadline) and dropped its entry at the answer, not the read's end; a deadline between a read's write and its RELEASE tore the snapshot; an absent table could be named `deadline`; a late error lost every fact; another tool's `schema_migrations` read as the fork's invisible ledger; a locked ledger turned the version row's warning into ✓ | run-it, cold read | keyed by surface and held to the read's end; the field leaves `pending` as it is written; absent tables settled first; a late error keeps what was read; only a ledger with `sha256` counts; the version row says could-not-verify; held by test-server [10c], test-e2e-sql [14], test-preflight |

**Not taken.** Estimated counts or a cache for the read: 1M thoughts and 1M
audit rows answer in 37–73 ms. A keyed probe registering its key in the agent
registry, as its first MCP request would: it is how revocation is honoured.
Folding the counts into fewer round trips: one transaction on a local database
answers in 17–34 ms; a remote one's round trips are measured before they are cut.

**Not done.** `server/index.ts` (the Edge Function build SMD-1800 retires) keeps
its `1.0.0` literal. The PostgREST store reports no database facts. Preflight's
`stats: false`, and the single-flight entry lasting to the read's end rather
than the answer, are held by no suite — a mutant that drops either passes; each
is a cost bound, not an answer. Follow-ups: SMD-2062 (preflight's unqualified
`ob1_config` read, older than this change), SMD-2063 (preflight's facts read
refused by a statement-mode pooler).
