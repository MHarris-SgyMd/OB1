---
type: added
bump: minor
tickets: [SMD-1730]
migrations: [045]
---

## Changelog

`thought_audit` becomes the log of record: every write stamps who holds the key (`actor_kind`, from the registry, never the payload), the ceiling on the content (`trust`), the door (`origin`), and what the call declared (`stance`, `cites`, `valid_from`, `valid_until`); the append-only rule gains its one lawful amendment, a backfill that fills a NULL kind and door and says so. Migration 045; `set_agent_kind` classifies a key, `backfill_thought_audit_events` fills the rows written before (SMD-1730).

## FORK

`thought_audit` becomes the log of record — who from the key, the door, the ceiling on the content, stance, cites and the valid window on every write; immutable by rule; migration 045 (SMD-1730)

**Premise.** 008 made `thought_audit` the one row every capture, update and delete leaves, written by a trigger inside the mutating transaction, UPDATE and DELETE refused by a second trigger; 010 put a stable agent id beside the key's name; SMD-1541 made the five vendored servers pass an actor at all. SMD-1729 makes this table the log the rest of the program derives its views from, and for that it lacked the fields those views read and one rule. Nothing said whether the key belongs to the operator, to an agent or to an importer copying external text, so a read that wants "what the operator said" (SMD-1726) or "how far to trust this" (SMD-1724) had nothing to filter on. The door a write came through lived in `actor_context.via`, a key in the blob 008 declares "analytical only". Nothing recorded what a write claimed to rest on, its stance, or when the fact it states was true in the world. And "derived from the key, never the payload" was a sentence in the epic and nothing in the schema.

**Two columns for the two questions.** `actor_kind` is *who holds the key*; `trust` is *the ceiling on the content* — the same three words, `operator | agent | ingested`, ranked in that order. The case that settles it, from SMD-1724: the operator's key handing in an ingested web page is actor `operator`, trust `ingested`, which one column read two ways cannot say. trust is never above the kind: undeclared, it *is* the kind; declared lower, the declaration stands; declared higher, it is clamped to the kind and the attempt is recorded on the same row under `actor_context.claimed`. A key of unknown kind supports no claim above the floor — only a declared `ingested`, a lowering nobody can abuse, is kept. SMD-1724 and SMD-1726 are label-and-filter work on these two columns; both said the decision was this ticket's.

**The kind lives in the registry, and the function is the enforcer.** `ob1_agents` (010) gains `kind`, set by `set_agent_kind(label, kind)` — an upsert by label, so a key can be classified before its first request; `resolve_agent` then meets a known label with an unknown digest, its rotation branch, and attaches the hash. The audit trigger reads the kind by the envelope's `agent_id` (010), or by the key's name for a writer that never resolved one (SMD-1541's five servers, the workers), and never from the payload: the database never sees the key, so the row the operator classified is the one thing a claim can be checked against. Declined: a fourth field on `MCP_ACCESS_KEYS`' `name:scope:sha256`. 010's rule is that the environment authenticates and the table records who; a kind in the env would be a claim the env does not enforce, parsed by seven byte-identical `auth.ts` copies and quoted by every README that shows the grammar. Declined too: a second environment variable keyed by name — two lists to keep aligned. NULL is the honest value for a key nobody has classified, and preflight's `audit events` counts the keys and the rows waiting on one, naming `set_agent_kind` and the backfill as the remedy. The server changes nothing about how it authenticates.

**`origin` is a second column, and `source` becomes one vocabulary.** `origin` is the door — SMD-1541's `via`, promoted from `actor_context` on 008's own reasoning for promoting a key ("promoting a column is a migration, promoting a JSON key is an archaeology exercise"); the trigger strips it from the blob as 010 strips `agent_id`. `source` keeps its name and its meaning narrows to the row's own `metadata.source`: the trigger no longer reads an actor's `source`. Until now the column carried three vocabularies, as SMD-1541 noted on this ticket — the main server passed `"mcp"` (a transport), the workers their own names (a door), the five vendored servers nothing (the row's origin) — so `WHERE source = 'mcp'` found the main server's edits and missed every MCP door under `integrations/`. Every in-repo writer now passes `via`: `server-portable` sends its MCP server name, `open-brain`, from one constant; `consolidation-bio`, `db/consolidate.ts` and `db/reembed.ts` moved their names from `source` to `via`. An out-of-tree actor still sending `source` sees it land in `actor_context`, visible, rather than silently interpreted. The main server's captures still carry `source: "mcp"` in the row's own metadata, so that column reads as before for them.

**The event rides one envelope key.** `p_payload.event` on both inserting `upsert_thought` forms (the 4-argument form delegates), and a tenth, defaulted `p_event` on `update_thought` — 032's mechanism for a new parameter, carried one form further: the ACL of whichever older form is there (9-, 8- or 7-argument) is captured, all three are dropped, the ACL is replayed onto the new form, so the last definer leaves one function whatever state it meets. The shape: `{stance, cites, valid_from, valid_until, trust, actor_kind}`, validated by `validate_write_event` — stance one of three words; cites an array of UUID strings naming thoughts that exist, canonicalised as `validate_derived_from` does; the window two timestamps in order; trust and actor_kind one of three words; a key the shape does not have refused, so a misspelling cannot vanish. A bad *shape* is refused as 025 refuses a bad `derived_from` — the write is the choke point or it is an untrusted-input hole. A *claim* the key cannot support is clamped and recorded, not refused — the write is legitimate, the label is not. The functions validate the event before any lock and write `ob1.event` immediately before their INSERT or UPDATE — after every refusal they can return, unconditionally, an empty string when none — and the trigger reads it once and clears it, so neither a later call nor a raw write in the same transaction can inherit it; `delete_thought` is not redefined — a tombstone declares nothing and the trigger reads no event on DELETE — but its rows gain `actor_kind`, `trust` and `origin` from the actor it has set since 009.

**Immutable by rule.** `thought_audit_refuse_mutation` (008) gains the one lawful amendment this migration needs: under the setting `ob1.audit_amend = 'backfill'`, an UPDATE may fill a NULL `actor_kind`, `trust` or `origin` and stamp `backfilled_at` — and only with what the row itself derives to: the kind the registry holds for the row's id (else its name), the door the row's own blob carries as `via`, the trust the write path's rule gives from the declaration the trigger filed under `claimed`, the stamp this transaction's `now()`; something must be filled; every other column byte-equal (`to_jsonb(OLD)` against `to_jsonb(NEW)` with the four removed); a value once set never changed; DELETE never, and TRUNCATE — which fires no row trigger, a door 008 did not mean to leave — refused by the same function as a statement trigger. The trigger itself knows which change is lawful, so nothing outside it — no grant, no `DISABLE TRIGGER` in a script — has to, and a hand UPDATE under the setting can write nothing the backfill would not; the setting is a key any role that can write the table may turn, and the rule is *what* may change, not who. SMD-1723's redaction — the audit rows' content replaced by a marker and a hash, with its own event row — is the second named amendment and arrives with that ticket; it goes in this function.

**The backfill is a function.** `backfill_thought_audit_events(p_limit)` fills `origin` from the blob's `via` (a string; anything else is no door) and `actor_kind` and `trust` from `ob1_agents` by `canonical_agent_id`, else by `actor_name` — an id the registry no longer knows falls back to the name, as the trigger does — stamping `backfilled_at`: 023's shape, callable again as kinds are set, `p_limit` bounding a pass, the UPDATE's WHERE re-evaluated on the locked row so two passes at once fill each row once and neither re-stamps the other's. The migration calls it once: every SMD-1541 row gains its origin now; no row gains a kind at apply time, because no agent has one yet. Each backfilled row says so itself — that is the backfill's record; no sentinel audit row is written, because an audit row names a thought. Trust on a backfilled row is the write path's rule applied late: the trust the write declared while its key was unclassified (filed under `claimed`, since the trigger could not honour it then) when the kind now supports it, else the kind — never a raise; the claim stays in the blob, because the row was written when it could not be checked and the row says so.

**Not here, said so.** MCP tool arguments for stance, cites, the window and trust: SMD-1733 (stance and cites, the grounding rule), SMD-1725 (the window, the `as_of` read) and SMD-1724 (trust, the label) give them semantics and add them; the store gains the argument with the first tool — plumbing with no caller is the pattern 025 declined for `thought_edges`. Facet events (042's comment on this ticket): a facet is a derived sidecar and SMD-1731's derivations table records its lineage; the event's `cites` is the record of what a *write* claimed to rest on, 042's rows the per-statement projection. Indexes on `origin`: no reader yet. The partition key is chosen and not applied — RANGE on `created_at` by month, the table being append-only so a closed month is cold; the table's COMMENT says so and SMD-1697's hundred-million bench decides when. Two partial indexes, on `actor_kind` and on `trust`, for the reads SMD-1724 and SMD-1726 build; the trigger's cost gains one primary-key lookup on `ob1_agents` per row.

**Version.** MINOR under the version rules: the columns are additive behind `IF NOT EXISTS`, and `update_thought`'s added parameter is defaulted, so every 9-argument call — the servers' by name, `reembed.ts`'s positional — resolves as before. The migration is the first past the planned first cut's range (`001..044`); the release step decides where it lands.

**The tests.** `db/test-schema.ts` [43] (the suite grows from 1084 to 1191 assertions, PGlite, at 1024 and at 768 dimensions): the eight columns and their CHECKs, the registry's kind, `set_agent_kind`'s four answers, the resolver meeting a pre-classified label; the settling case; the Verify fixture — an agent key claiming `operator` twice, stored `agent, agent` with both claims under `claimed`; the label fallback; an unclassified key honouring only a lowering; an actor's `source` landing in the blob; the 2- and 4-argument forms and `update_thought`'s `p_event`; no inheritance inside one transaction and a tombstone declaring nothing; ten refusals of a bad shape; the amendment gate refusing an UPDATE of `diff`, a change to a set value, a fill without the stamp and a DELETE, and admitting the one lawful UPDATE; the backfill on a SMD-1541-shaped row and on rows whose key is classified after the fact, `p_limit`, idempotence. Sections [22], [31], [32], [33] and [35] re-pin the last definer and re-apply 032 and 033 on a brain without 045's form, since a re-apply beside it is the ambiguity 045's DROP chain exists for. `db/test-upgrade.ts` [20b]: 045 onto a populated 044 — the SMD-1541 row gains its origin from the blob, the main server's row is left as it was, 008's and 010's columns byte-equal on every row, no thought moved, the append-only trigger still refusing after the ALTER (010's lesson), one `update_thought` of ten with the 9-argument form's ACL across the DROP, a 9-argument call still resolving, a classified key stamping its kind and the backfill classifying the rows written before; the window guard re-armed at sixteen. `server-portable/test-preflight.ts`: the `audit events` check's six arms — present, the trigger body from before 045, an unclassified key with its remedy, ok once classified, a column dropped from under 045's trigger (the failure), the same column missing under 025's trigger (the warning) — the capture role refused without SELECT on `ob1_agents` after a real capture fails in the trigger, and the `edit signature` check's new states: a 9-argument form beside the shipped one after a hand re-apply of 032 or 033, refused with the one DROP, and a 9-argument form alone as the WARN it is. `extensions/test-writes.ts` reads the door from the `origin` column and asserts `via` is no longer in the blob, for all thirteen SMD-1541 arms and the bio worker's two; `test-auth.ts` [10] asserts the wire form carries `via` and no `source`.

**Review pass 1** (a cold read of the diff at high effort beside a run-it reviewer), eight findings from the read, all fixed. The audit trigger, SECURITY INVOKER, now reads `ob1_agents` on every write that carries an actor, and `ROLE_GRANTS.capture` — the set preflight's `write privileges` enforces — had no SELECT there: a role granted exactly the capture set passed preflight and failed every capture, edit and delete inside the trigger; the row is in the capture group now, the README's table and the check's sentence name the trigger, and test-preflight revokes it from its capture role, watches a real capture fail with `permission denied for table ob1_agents`, and watches preflight refuse. A tombstone still read the previous call's `trust` and `actor_kind` from `ob1.event` — only stance, cites and the window were masked — so a delete after an event-bearing capture in one transaction was stamped a trust it never declared; the trigger now reads no event at all on DELETE, and reads it ONCE otherwise, clearing the setting, so a raw `UPDATE thoughts` later in the same transaction (an enhanced-columns write beside a capture) inherits nothing either. The backfill wrote the kind over a lower trust the write had declared while its key was unclassified — the trigger had filed the declaration under `claimed` because it could not honour it — a raise by the back door; the backfill now applies the trigger's rule late, the declaration standing when the kind supports it. The amendment gate let a hand UPDATE under the setting put trust above the kind, which the write path refuses; the gate holds the rule. An `agent_id` the registry no longer knows (a server's cached id across a registry rebuilt by hand) fell through to no kind with no name fallback, in the trigger and the backfill alike; both fall back to the name when the id finds no row. `validate_write_event` carried a second copy of `validate_derived_from`'s rule for cites; it calls the one rule and re-raises its refusals naming the event's field. And preflight's `audit events` called a brain without 045's columns a failure that would fail every write — on that brain the trigger is 025's and every write goes through — so a missing column is a WARN naming 045 unless the trigger body is 045's, when it is the failure it says. Not taken: nothing.

The run-it reviewer (the real migrator on a populated 044 brain, `--reapply`, preflight's lines in every registry state, forty-odd edge inputs, the gate under adversarial UPDATEs, two sessions, the trigger's cost, `reembed.ts` and `consolidate.ts` on the brain, the PostgREST shim, a static sweep of every positional caller) confirmed the read's two bugs on the committed file and found six more, five fixed. The amendment gate admitted a back-dated stamp, a re-stamp with nothing filled, and an invented door — the gate could not know provenance; it can, and now every amendment must equal what the row derives to (the registry's kind for its key, the blob's `via`, the rule's trust from the filed claim), must fill something, and must stamp this transaction's time. Two backfills at once re-stamped and re-counted every row the other had filled; the UPDATE's WHERE is re-evaluated on the locked row, so a row filled meanwhile is skipped, and test-upgrade holds one pass open while another runs. A caller's own `claimed` key in its actor was written over by the trigger's record; they merge. `validate_write_event` took Postgres's own words for a time — `now`, `today`, `infinity` — so a client's `"now"` would have landed as the call's time and read as a fact about the world; the window takes strings that begin as a date does, and a refusal echoes at most 500 characters where a 100,000-character cite came back whole. A `via` that was a number or an object became a door spelled as JSON; a door is a string, anything else stays in the blob. And `TRUNCATE thought_audit` fired no row trigger since 008; the same function refuses it as a statement trigger. The census named nobody when no key was unclassified though 9,008 rows waited: it names the actor names that wait and counts the rows whose key is classified since and want only the backfill. Left as designed, said here: an edit that changes nothing, or a re-capture of stored text, writes no audit row — 008's rule — and so records no event it declared; the event rides a write, and the caller's `ok`/`existed` says the write did not happen. `db/reembed.ts` sends eight positional arguments, not nine; the message said nine. The trigger's cost against 025's body: inside the noise on captures with a vector (2.66 vs 3.26 ms a capture, table growth dominating), about 0.005 ms a row on bare inserts.

**Review pass 2** (a fresh cold read at high effort; a run-it reviewer aimed at what pass 1 changed), eight findings from the read and five from the run, all but two taken. From the read: `update_thought` wrote `ob1.event` before its refusals, so a call that returned NOT_FOUND or STALE_READ fired no trigger to consume the event and left it for a raw write later in the transaction — the run-it confirmed it on a real brain; the functions now validate early and write the setting immediately before their INSERT or UPDATE, after every refusal, so a refused call leaves nothing. The trust ladder and the id-then-name registry lookup were each spelled three times — in the trigger, the gate and the backfill — while the gate's correctness rests on the three agreeing byte for byte; they are `ob1_trust_ceiling` and `ob1_registry_kind`, one copy each (and `ob1_door_of` for the blob's `via`), called from all three, and the suite pins that they are. preflight's `write privileges` required SELECT on `ob1_agents` unconditionally, so a role with the pre-045 capture set was refused on a brain still at 044 where 025's trigger reads nothing there; the requirement follows the trigger body, read by its sentinel, as 016's follows its trigger's presence. The `audit events` census ran three unindexed scans of the log at every start; it is one pass over the waiting rows, under a new partial index on them by name, and it resolves each row as the backfill does — by id, else by name — so "waiting only on the backfill" is what the backfill would fill (the run-it found a by-name count promising a fill the id refused). The `edit signature` arm for two older forms and no shipped one named 032 as the remedy, which would have left its own 9-argument form to be named on the next start; any 9-argument leftover names 045, whose DROP chain reaches all three. Two vendored servers' comments still said the door stays in `actor_context`. And the read asked what "from the key, never the payload" guarantees when a direct SQL caller can name another key's label in its actor: the boundary is stated in the trigger — the envelope's name and id are the *server's* word, trusted as 008 and 010 trust them, and a caller who can call the function directly can already write any content it likes; what the trigger enforces is that the *event's* claims cannot exceed the envelope's identity, and no MCP client composes the envelope. From the run: a row a tool INSERTed with a kind and no trust poisoned the backfill — the gate demanded the derived trust, the backfill left it NULL, and every later pass on the whole brain raised; the backfill now fills every NULL the gate would admit, trust from the kind the row has or gains included. The gate's one sentence served thirteen different refusals; it names the condition that failed. An empty-string `via` was a door; a door is a non-empty string. The TRUNCATE refusal named the row trigger; it names its own. A role granted exactly the capture set was warned on every start that the census could not be read — INSERT on the log, not SELECT, is the capture set's; it is a skip naming the community group's SELECT. Not taken, with reasons: BC dates in the window stay accepted — a fact about the Roman Republic has a window too, and the calendar is Postgres's; a pre-set trust above the kind on a raw INSERT stays, a set value being never changed, and the trigger never writes one.

**Review pass 3** (a fresh cold read at high effort; a run-it reviewer aimed at pass 2's changes), ten findings from the read and a handful from the run, all taken but one. The trigger probed the registry on every write, actor or none — a raw writer with no `ob1.actor` set would have needed SELECT on `ob1_agents` for a lookup that could only answer NULL; it probes only when an envelope names an id or a name, and test-preflight drives a raw actorless INSERT as a role without that SELECT. One refusal still came *after* the event was written: `update_thought`'s UPDATE can match no row (the `if_unchanged_since` predicate re-checked under the lock) and return STALE_READ with nothing having consumed the setting; that arm clears it. The backfill derived each row's kind under its statement's snapshot while the gate re-derived it under a fresher one, so a `set_agent_kind` committed mid-pass would have made them disagree and rolled the whole pass back with no hint; the backfill holds the registry rows FOR SHARE for the pass, so a reclassification waits for it instead. `ob1_registry_kind` consulted the name only when the id was unknown, never when the id was known but unclassified — exactly the row a key renamed in the env and pre-classified under its new name leaves behind when 010's rename branch meets `label_conflict`; it is the id's kind, else the name's, one primary-key probe when the id is classified (the run-it found the first form probed the id twice and was not inlined, having sub-selects; the comment said it was). The census counted every unclassified registry row, so a key retired through `revoke_agent_key` kept the warning lit for ever; it counts a key only while it holds an unrevoked digest, groups the waiting rows by writer before resolving them, and when every waiting row names a classified key its remedy is the backfill alone. The backfill's WHERE was an OR over function results no index could serve, a full scan per pass on a filled log; each arm now begins with the predicate of a partial index — the awaiting-kind index widened to rows waiting on a trust, and a new awaiting-door index, empty once this file's own call has run — so a pass on a filled log reads indexes, not the heap, and a limited pass no longer walks the whole log from the oldest row to find the newest waiting ones. The concurrency arm in test-upgrade passed identically whether or not pass B was blocked — Bun runs a query when it is awaited, not when it is written, so B had never been in flight before A committed; B is executed at once and the arm asserts B was seen waiting on A's lock in `pg_stat_activity` before A commits, and dropping the re-evaluated WHERE now fails it. From the run: the census skip named `thought_audit` as the table to grant when the denial was on `ob1_agents` (it names the table the error names); every `edit signature` arm that finds no shipped form now remedies with 045, whose DROP chain reaches every older form, where 032 would have left its own 9-argument form to be named on the next start; a stale comment in `update-thought-mcp`; and pass 2's sentence "a refused call leaves nothing on the transaction" overstated — the *event* leaves nothing; the *actor*, 008's and set before the locks, stays, by 008's design. Stated, not changed: a hand-set `ob1.event` before a raw write stamps that row — the setting is the same trust boundary as `ob1.actor`, a role that can write the table can set either; a caller's own `claimed.trust` in its actor is read by the backfill as a declaration — only ever a lowering, since the ceiling clamps a raise. And CLAUDE.md's guard rail names the word TRUNCATE in SQL files, while this file's one occurrence is the statement trigger that refuses it: the rail is a data-safety rule inherited from upstream's contribution process — a pasted SQL file must never destroy a stranger's rows — and the four forbidden statement shapes are a proxy for that a grep can enforce; a refusing trigger is the rule applied. Decided with the operator (2026-09-22): the trigger stays, and the rail in CLAUDE.md and CONTRIBUTING.md now says a refusing trigger is not a breach. Whether the rail should be enforced mechanically — the fork has no gate that greps for it, and a literal grep would flag this file — is SMD-1936: decide the rule's scope, then a check beside 7 and 12 whose parser tells a refusing trigger from a destroying statement.

**Review pass 4** (a fresh cold read at high effort; a run-it reviewer aimed at pass 3's changes), six findings from the read and one confirmed bug from the run, all taken. The run reproduced first try what the read predicted: `PERFORM … FOR SHARE` locks the registry rows that exist, so a label with no row yet — `set_agent_kind`'s whole point is to create one before the key's first request — was inserted and committed mid-pass, the candidates had derived NULL, the gate derived the new kind, and a 20,000-row pass rolled back with no hint. The gate now accepts each of the three columns either left as it was or set to what it derives to — a partial fill is not an invention, and the next pass finishes it — while the FOR SHARE hold stays for the case it does cover, a reclassification; the hold's other cost is stated: 010's rename branch waits behind a pass, so a key renamed in the env stalls on every request until the pass commits, which is why a large log is walked in `p_limit` batches. A re-capture of stored text, or an edit that changed nothing, silently discarded the event it declared — checked by `validate_write_event`, then lost on 008's empty-diff guard; an unchanged write that declares an event is now an event, one row with an empty diff and the declaration on it, which is the restatement SMD-1722 counts, while a bare re-import still writes nothing. `db/reembed.ts` resolved the body by the ten-argument signature but diagnosed its absence as 032's, telling a brain at 044 to re-apply the wrong migration; it names 045 when 032's form is there. A row carrying an id and no name could be stamped by the trigger but never filled or counted — the backfill's arm and the census required a name; both take an id or a name, and the awaiting index's predicate with them. A window with no offset was read in the session's TimeZone, so two servers over one brain recorded different instants for one declaration; an offset-less input is UTC, an offset — one that follows a time of day, not a bare date's own dash — is honoured. The `audit events` check on a brain with no `thought_audit` at all said the brain predated 045; it skips and points at the audit-trail check, which names 008. From the run's smaller findings: `actor ? 'name'` is true for a JSON null and probed the registry for nothing (`actor->>'name' IS NOT NULL` now); the second STALE_READ arm's clear is unreachable under READ COMMITTED with the row locked and is described as the belt to that brace, not as a live refusal; the backfill's `ORDER BY created_at` invited the planner, when many rows waited and were newer than the filled mass, to walk the created_at index from the oldest row — which order a bounded pass takes is not a contract, and the ORDER BY is gone; two "leaves nothing on the transaction" phrasings became "leaves no event". Measured by the run: with 200,000 filled rows and 500 waiting, a bounded pass is a BitmapOr over the two partial indexes at about a millisecond, and the census under a millisecond; a pass fills about 0.025 ms a row.

**Verified.** Four mutants on the real migration file after review pass 4, each a full `bun test-schema.ts` run (1191 assertions), the file rebuilt from 025/033/035 by script after each. The trigger made to trust the payload's `actor_kind` (`v_kind := COALESCE(event->>'actor_kind', v_kind)` after the registry lookup) — the ticket's first mutant: 1189/1191, the two arms that fail are the Verify fixture (an agent key stored as `operator, operator`) and its `claimed` record. The refusal trigger made to pass any UPDATE under the setting — the ticket's second: 1183/1191, the UPDATE of `diff`, the change to a set `actor_kind`, the fill without the stamp, the kind the registry does not hold, the trust above the kind, the invented door, the back-dated stamp and the re-stamp with nothing filled all go through (this run also caught an arm of pass 4's own that read "the latest" update row by `created_at` — PGlite's `now()` is millisecond-grained, two statements tied, and the arm passed by luck; it counts a marked row now). `ob1_registry_kind` made to read the id only — the trigger, the gate and the backfill all lose the name at once, which is the point of one copy: 1180/1191, the eleven arms that depend on a writer with no `agent_id`, with one the registry does not know, or with one it knows but has not classified, in the write path, the lawful amendment and the backfill alike. The trigger made to keep the event it read instead of clearing it: 1190/1191, exactly the arm in which a raw `UPDATE thoughts` follows an event-bearing capture and a tombstone in one transaction — the arm that had gone blind in pass 2's first draft, when a block inserted before its assertion changed which row it read; a mutant that had bitten and stopped is how that was found. A fifth, on Postgres: the backfill's re-evaluated WHERE removed — test-upgrade's two-passes arm, now proven to have B waiting on A's lock, errors out, because B re-stamps the row A filled and the gate refuses it with "nothing is filled". The functions' own unconditional write of the setting has no mutant of its own: with the write immediately before the INSERT or UPDATE and the trigger clearing on read, every inheritance path the suite drives is closed by the trigger, and the functions' write is the belt to that brace. Every other suite on the tree: `test-schema` at 1024 and at 768, `test-upgrade` 258, `test-live` 579, `test-audit` 20, `test-agents` 68, `test-update-delete` 62, `test-store-sql` 120, `test-e2e-sql` 115, `test-store-postgrest` 115, `test-preflight` 307, `test-tools` 122, `test-search-path` 23, `test-auth` 809 (extensions) and 67 (server-portable), `test-writes` 281, `check-fork-consistency` PASS, `tsc` clean.

**Upstream status.** Not upstream. Upstream's `schemas/thought-audit` has no kind, trust, origin, stance, cites or window, and no key registry to derive a kind from.
