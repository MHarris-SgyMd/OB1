---
type: changed
bump: patch
tickets: [SMD-1802]
migrations: []
---

## Changelog

The docs and the repo rule are off Supabase: CLAUDE.md's guard rail reads "remote over HTTP, never stdio" and names no host, `primitives/deploy-edge-function` is `primitives/deploy-remote-mcp` (the `bun <file>` run, an access key, TLS in front), every deploy section under the category directories runs its server that way and points at it, and `docs/01-getting-started.md` walks the compose stack in `SETUP.md` instead of a supabase.com project (SMD-1802).

## FORK

Docs and policy off Supabase — the rule keeps its intent and drops the host, one primitive shows the run, and the getting-started guide builds the fork's own stack (SMD-1802)

**What changed.** Four commits and the review passes' three, prose and
metadata alone; no code path moves but the checker's two exceptions for a
guide that no longer has SQL.

1. The policy. `CLAUDE.md`: "one database (Postgres + pgvector)"; the
   guard rail reads "MCP servers must be remote over HTTP — a URL a client
   is pointed at — never a stdio process or a local server a client spawns",
   names the Bun container in `SETUP.md` as the reference deployment and
   `bun <file>` as how every extension, recipe and integration server runs.
   `CONTRIBUTING.md`: the numbered-commands example, the GRANT step (the
   connecting role, not `service_role`), the "Remote MCP setup" requirement
   and automated rule 14 say "one HTTP process under `bun <file>`; its tests
   import it"; the PR template gains that checklist line. `FORK.md`
   "Deploying" says the sweep is done. The root README's getting-started
   line points at the guide as the beginner's walk through `SETUP.md`'s
   stack (the Vimeo link to upstream's Supabase walkthrough goes) and says
   upstream's AI assistants answer for upstream's deployment; `SETUP.md`'s
   opening stops disowning the guide.
2. The primitives. `deploy-edge-function` → `deploy-remote-mcp`, "Run a
   Remote MCP Server": the extension's schema, the pinned install, Step 3
   minting a key (the anchor every README links survives), the run line,
   Step 5 putting it behind HTTPS (a proxy or tunnel on the host, or any
   host that runs Bun and reaches Postgres), updating, troubleshooting.
   `remote-mcp` and `troubleshooting` rewritten for the same shape: URLs
   `http://127.0.0.1:8787/mcp?key=` on the machine and `https://your-host/`
   through the proxy; "read the server's output" where the dashboard's Logs
   tab was; no cold starts, no project region, no `supabase secrets list`.
   Thirty-three files' links and `requires_primitives` renamed (35 slug
   references, 27 link titles), `primitives/README.md` and the root table's
   row reworded.
3. The sweep. The six extensions and the template (`README.md`,
   `AGENT_SPEC.md`): prerequisites name Bun, trackers hold a Postgres URL,
   schema steps say `psql -f` with the Supabase SQL Editor as the option
   for a Supabase-hosted Postgres, meal-planning's household boundary is
   the shared server's own key list. Fourteen integrations' deploy sections
   (seventeen integration directories touched in all): the "Runs
   under Bun, not as an Edge Function" callout becomes the run step, the
   `supabase functions new`/`secrets set`/`deploy` blocks and
   `*.supabase.co/functions/v1/…` URLs go; the Slack and Telegram READMEs'
   inline servers take the Bun shape (`process.env`, `export default
   { port, fetch }`, the SQL shim by relative path; parsed with `bun
   build`, not run — SMD-2101). Nine recipes' deploy sections (fifteen
   recipe directories touched in all; editorial-policy's schedule
   is a cron line, `schedule.sql` kept for a Postgres with `pg_cron`;
   brain-smoke-test carries a note that its PostgREST categories have
   nothing to talk to here — SMD-2103), two recipe `.env.example`s, two
   schemas' `supabase db push` lines, two skills, the Hermes and OpenClaw
   endpoints and the Hermes plugin's own hint (`/functions/v1/` was never a
   route here), six schema READMEs, the two Next dashboards' remaining Edge
   Function lines, the RLS primitive's Supabase-only steps, `docs/03-faq.md`,
   `docs/05-tool-audit.md`, the GPT context, `compat/`, `server-portable/`
   (its dead `../server/` link) and `deploy/` READMEs. Thirty-two
   `metadata.json`: `tools` "Supabase CLI"/"Deno" → "Bun 1.4+", `services`
   "Supabase" dropped, `edge-function` tags dropped.
4. `docs/01-getting-started.md` rewritten (976 → 577 lines), eight steps
   on the compose stack: install podman/Docker, Bun and Git; clone; fill
   `deploy/.env` (password, `bun keygen.ts`, `OB1_LLM_LOCAL=1` — without it
   a capture lands with no vector — or OpenRouter's six lines, the egress
   policy among them); `compose
   up --profile local-models`, reading `preflight OK`; `deploy/smoke.sh`
   and the keyed `/health`; the URLs, with a tunnel for hosted connectors;
   the seven client sections kept with the fork's URLs; use it;
   troubleshooting from the server's log. `docs/04-ai-assisted-setup.md`'s
   wizard reads this repository, not upstream's, and its human parts are
   installers, not dashboards. `docs/video-walkthrough-script.md` deleted.
   `scripts/check-fork-consistency.ts`: the guide's two counted exceptions
   (checks 7 and 10, for SQL it no longer shows) removed.

**Why.** `SETUP.md` and the guide disagreed: the guide built a supabase.com
project and `curl`ed *upstream's* `server/index.ts` into it — a reader who
followed it built upstream's system, not this fork's — while every server
under the category directories had been Bun-native since SMD-1799 and the
Edge Function build gone since SMD-1800. The ticket's grep counted 46 live
Markdown files instructing a deploy that no longer worked; the repo rule
that governed contributions still named the host the fork had removed, and
SMD-1806, SMD-1794 and SMD-1298 were waiting on its rewording.

**Held.** `check-fork-consistency` PASS on the tree: checks 1 and 4 (the
metadata validates; every `requires_primitives` names a directory), check 3
(relative links), check 19
(`services` after the drops), checks 7 and 10 with the guide's exceptions
gone. `scripts/` typechecks. The ticket's Verify grep — `supabase functions
deploy|supabase/functions/|Supabase Edge Function` over `*.md` — matches
FORK.md and ten `changes/` files, the record (nine earlier records and this
one): 53 files before, 11 after, none live. Not held in CI: a doc may name upstream's deploy as history, and
the record does (Not taken).

**Measured after.** The grep above. The rewritten primitive's Step 3 anchor
is linked from 17 files and resolves; the four dead links the link pass
found predate this change (README's contributions table, CONTRIBUTING's
GitHub-relative issue templates).

**Review passes.**

| Pass | Finding | Caught | Fix |
| --- | --- | --- | --- |
| 1 | the guide said `smoke.sh` proves a capture (it is read-only), sized the models at 2.5 GB (7 GB), counted 10/7 tools (12/9), gave a Windows one-liner that throws under Bun, an `ls` that hides `.env.example`, a log line preflight no longer prints, and a re-embed command the host cannot run; the server does not wait for `ollama-pull`; SETUP.md and deploy/README.md carried the old counts and the legacy single key | cold read (one reviewer, run against the tree) | the pass-1 commit |
| 1 | the FAQ named a preflight row that does not exist; the primitive called every extension schema idempotent (two are not); an orphan Supabase URL block; nine Edge Function sentences the ticket's grep does not match; three counts in this record | cold read (second reviewer, the diff) | the pass-1 commit |
| 1 | five extension trackers still carried project-ref and secret-key rows, three integrations said "Supabase secret", two dashboards and the RLS primitive still named Edge Functions or the SQL Editor as the only way | grep (the wider phrase set) | the pass-1 commit |
| 2 | the guide's hosted-models option set OpenRouter but not the egress policy — with the gate's `deny` default every capture lands with no vector, the opposite of what the paragraph promised; `docker compose ps` hides the exited pull container; the python.org installer provides no `python3`; Docker Desktop's host name on a Mac | cold read of pass 1's fixes, run against the tree | the pass-2 commit |
| 2 | CONTRIBUTING said "its tests import it" but `test-auth.ts` starts a hand-kept list and refuses a server it does not know — nothing told a contributor to add theirs; `tailscale serve` offered for a public URL in three docs (it is tailnet-only); seven extension callouts sent readers to a connection string the guide never gives; "a header on the URL" | cold read, the maintainer's lens | the pass-2 commit |
| 2 | six documents cited the old guide's step numbers (Step 2.x the SQL, Step 5 the key); the FAQ over-claimed the SvelteKit dashboard edits thoughts; the ingest-endpoint and gmail-smart-pull sentences pass 1 wrote described code paths that do not exist | grep; cold read | the pass-2 commit |

| 3 | pass 2's "the models survive `down -v`" was false (both named volumes go); "add to `LIVE`" overstated (it derives from `SERVERS`); the env example's section is "What may leave the box", not "Egress"; `/health` answers a bare `ok` to a key it does not know; the no-vector entry knew only the local path | cold read of pass 2's fixes, run against the tree | the pass-3 commit |
| 3 | rest-api's `/ingest` was said to proxy to a smart-ingest server here — it proxies to `${SUPABASE_URL}/functions/v1/…`, nonsense with the shim's URL (SMD-2114); rest-api and smart-ingest were said to import the shared auth module and compare by digest (their own helpers, constant-time on the raw key); the editorial-policy run line lacked the Slack variables its scheduled run posts with; the two dashboards' ingest entries named a server their gateway never calls; the shared-mcp primitive's boundary test used supabase-js against RLS the shim does not enforce | cold read, the flows against the code | the pass-3 commit |

Pass 2's top findings sat inside pass 1's work (the hosted-models block pass
1 re-verified and left; the sentences it wrote) — the stop signal. Pass 3,
at the user's call, found one false sentence pass 2 wrote and the flows
above; the rest was pre-existing.

**Not taken.** A CI grep for the Supabase phrases (a doc may name upstream's
deploy as an example or as history — `recipes/content-fingerprint-dedup`,
`skills/ob1-local-http` — and the record does) — the record and FORK.md
name them by design. Real files for the Slack and Telegram samples — code
with tests, a PR of its own (SMD-2101). The five `.xlsx` trackers and guide
workbooks — binaries the sweep cannot edit, upstream's Supabase path,
unlinked now (SMD-2102). brain-smoke-test's shape (SMD-2103), and
`skills/auto-capture-claude-code`, whose script posts to upstream's
`/functions/v1/open-brain-rest/ingest` path (its fork note says so;
SMD-1298's follow-up). The
`supabase` tag on two recipes whose own tables live in a Supabase-hosted
Postgres: a label, not an instruction.

**Follow-ups.** SMD-2101, SMD-2102, SMD-2103, SMD-2114 (rest-api's proxy
target), filed with this. SMD-1846
(one origin for the stack) is where Step 5's "a proxy fronts every port"
becomes one path per server.

**Upstream status:** not sent — the fork dropped parity (SMD-1924);
upstream's docs are its Supabase path and stay so.
