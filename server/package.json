{
  "name": "ob1-server-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test-stateless.mjs && node test-stats-pagination.mjs && node test-capture-atomicity.mjs"
  },
  "comment": "Versions are pinned EXACTLY to server/deno.json so the test exercises the same libraries the Edge Function deploys with. @hono/node-server has no deno.json counterpart (test-only shim) and is pinned for reproducibility. If you bump deno.json, bump this file in the same commit.",
  "devDependencies": {
    "@hono/mcp": "0.1.1",
    "@hono/node-server": "1.19.11",
    "@modelcontextprotocol/sdk": "1.24.3",
    "hono": "4.9.2"
  }
}
