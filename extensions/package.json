{
  "name": "ob1-extensions-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test-auth.ts",
    "test:writes": "../db/with-postgres.sh bun test-writes.ts",
    "test:tools": "../db/with-postgres.sh bun test-tools.ts",
    "sync-auth": "for d in _shared ../recipes/_shared ../recipes/editorial-policy/_shared ../recipes/edge-function-cost-optimization/examples/_shared ../integrations/_shared ../integrations/consolidation-workers/_shared; do cp ../server-portable/auth.ts \"$d/auth.ts\"; done"
  },
  "comment": "Test-only (SMD-1252, FORK.md change 64): test-auth.ts imports the seven extension servers, and since change 67 (SMD-1455) the vendored recipes' and integrations' servers, as deployed; test-writes.ts (change 69, SMD-1228; change 71, SMD-1524) drives the vendored writers of a thought's content or vector against a real Postgres; test-tools.ts (change 77, SMD-1588; SMD-1798) drives every tool of the eight MCP servers with a schema of their own — seven extensions and the ob-graph recipe — against those schemas. Versions are pinned EXACTLY to the extensions' deno.json — one file, six identical copies — and to server-portable/package.json, so the test exercises the libraries the functions deploy with; test-auth.ts fails if any of those deno.json files, or a recipe's or integration's npm pin, drifts. Bump all of them in the same commit. No types package: bun runs the test untyped, and nothing type-checks this directory. `bun run sync-auth` rewrites every _shared/auth.ts from server-portable/auth.ts; the test names the six and fails if one differs.",
  "devDependencies": {
    "@hono/mcp": "0.3.2",
    "@modelcontextprotocol/sdk": "1.30.0",
    "hono": "4.13.8",
    "zod": "4.6.5"
  }
}
