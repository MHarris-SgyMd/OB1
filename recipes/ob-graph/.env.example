SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# name:scope:sha256 entries — the hash of each key, never the key; mint one as
# primitives/deploy-edge-function Step 3 shows. The older single MCP_ACCESS_KEY still works.
MCP_ACCESS_KEYS=laptop:write:sha256-of-your-key
DEFAULT_USER_ID=your-uuid-here
